# 상용 배포 전 검수 (2026-07-31)

브라우저에서 **실제 UI 코드 경로**로 돌려 잰 결과. 코드를 읽고 추론한 것은 여기 적지 않는다 —
전부 재현 명령과 수치가 붙어 있다.

재현:
```bash
npm run check                       # 유닛 167 + 밸런스 게이트 + 스프라이트 알파(_raw·public/assets)
npm run build && npm run preview    # 프로덕션 번들로 검수한다(dev 서버는 HMR 이 끼어든다)
QA_URL=http://localhost:5184 npm run qa:launch          # 실제 브라우저 플레이 (~20분)
QA_URL=http://localhost:5184 node tools/capture.mjs     # 뷰포트 3종 스크린샷
npx vitest run tests/learning.spec.ts                   # 종단 학습 실측표
```

`qa:launch` 는 서버가 떠 있어야 하고 20분쯤 걸려서 `npm run check` 에 넣지 않았다.
**배포 전에 한 번은 돌린다** — 일시정지 중복 종료(1-2) 같은 버그는 여기서만 잡힌다.

---

## 1. 고친 것 (전부 실측 전/후 있음)

### 1-1. DDA 부호가 뒤집혀 있었다 — 막힌 아이에게 더 어려운 문제
`src/edu/mastery.ts` `pickLevel()` 이 `theta + ddaLevel * 70` 이었다. 주석에는
"DDA 단계당 쉬운 쪽으로"라고 적혀 있었으니 의도와 정반대다.

| | DDA 0단계 | 1단계 | 2단계 | 3단계 |
|---|---|---|---|---|
| A2 · θ1400 체감 정답률 | 0.83 | 0.78 | 0.72 | **0.64** |

두 번 연속 틀린 아이(=DDA 상승)에게 더 어려운 문항이 갔다. **화면에는 아무 표시도 안 난다** —
레벨은 프롬프트에 드러나지 않으므로 플레이로는 영원히 발견할 수 없었다.
상수를 `economy.ts` 의 `DDA_STEP_ELO` 로 통일했다(70을 두 곳에 적어 둔 것이 사고의 원인).
회귀 방지 = `tests/learning.spec.ts` 의 「DDA 단계가 오를수록 체감 정답률이 떨어지지 않는다」.

### 1-2. 일시정지할 때마다 rAF 루프가 하나씩 늘었다 — 주간 점수 부풀림
`loop()` 은 일시정지 중에도 맨 앞에서 자기 자신을 계속 예약한다(멈추는 건 시뮬 갱신뿐).
그런데 `togglePause()` 가 재개할 때 `requestAnimationFrame(loop)` 을 **한 번 더** 걸었다.
잠깐 버튼을 N번 누르면 사슬이 N+1개가 되고, 판이 끝날 때 `finish()` 가 N+1번 실행된다.

실측(프로덕션 빌드, 실제 브라우저):

| | 저장된 판 수 |
|---|---|
| 일시정지 0회 | 1 |
| 일시정지 5회 | **6** → 수정 후 **1** |

`edu.rounds`·`edu.playMs`·**주간 순위 버킷**이 같이 부풀었다. Esc 키도 같은 경로라
아이가 우연히 밟기 쉽다. 순위표에 치트 방어를 넣어 둔 제품에서 **클라이언트가 자기 점수를
N배로 만드는 경로**였다.
원인을 막은 뒤, 저장에 쓰는 경로라 `finish()` 자체에도 `finished` 잠금을 걸었다
(관문 화면이 이미 쓰던 방식) — 다른 이유로 두 번 들어와도 안전하도록.

### 1-3. 복습(SRS)이 20판쯤부터 조용히 멈춰 있었다
`session.next()` 가 `dueQueue(전체, 8)` 로 **먼저 8개를 자르고** 그 다음 이 판의 유형으로
걸렀다. 밀린 항목이 쌓일수록 상위 8개가 죄다 다른 단원이 되어 필터가 전부 걸러낸다.

| 판 | 기한 도래 항목 | 상위 8개 중 이 판 유형 |
|---|---|---|
| 8판 | 24 | 8 |
| 16판 | 89 | 1 |
| **20판** | **129** | **0** |
| **28판** | **239** | **0** |

`srs.ts` 가 자기 파일 주석에 경고해 둔 "고정 slice" 함정이 **호출부에서 되살아난** 형태다.
거르고 나서 자르도록 바꾸고, 복습이 반대로 화면을 독점하지 않게 3문항에 1개로 끼웠다.

| 복습 비율 | 서로 다른 식(30판) | 오답 재출제율 | 학습중 졸업 |
|---|---|---|---|
| 수정 전 | 287 | 사실상 정지 | 17% |
| 1/2 | 220 | 84% | 26% |
| **1/3 (채택)** | **257** | **72%** | **22%** |
| 1/4 | 280 | 64% | 19% |

### 1-4. 폰 가로에서 숫자패드 배열이 부서졌다
`max-height: 430px` 에서 패드가 6열로 접히는데 DOM 순서가 그대로 흘러
`7 8 9 ⌫ 4 5` / `6 힌트 1 2 3 0` 이 됐다. '1'을 찾으려면 둘째 줄 힌트 옆을 봐야 하고
⌫가 9와 4 사이에 낀다. 아이는 숫자패드 위치를 손이 기억해서 누른다.
`data-k` + CSS `order` 로 두 줄 오름차순(`1 2 3 4 5 ⌫` / `6 7 8 9 0 힌트`)으로 다시 세웠다.
확인 = `screenshots/fix-phone-battle.png`.

### 1-5. 성 이름표가 밝은 배경에서 사라졌다
`우리 성`/`엉킴 성` 이 흰 글씨로 배경 그림 위에 그대로 얹혀 있었다. HP 바에는 `#0008`
테두리가 있지만 글자는 바 **밖**(`by-6`)이라 보호를 못 받는다. 구름 위의 '엉킴 성'이 안 읽혔다.
`strokeText` 로 어두운 외곽선을 넣었다.

### 1-6. "어디로도 보내지 않아요"가 조건 없이 적혀 있었다
주간 순위 동의 화면의 문구는 정확한데(별명·이번 주 숫자만, 이름/나이/학교/연락처 없음),
**메인 메뉴**와 **내 기록** 화면이 무조건 "기록은 이 기기에만 저장돼요 / 어디로도 보내지
않아요"라고 말했다. 순위 올리기를 켠 순간 거짓이 된다 — 특히 내 기록 표의 '푼 수'가
그대로 순위 점수로 나간다. 동의 상태에 따라 문구를 바꿨다.

### 1-7. 지도의 수문장 칸이 "× 10. 수문장"으로 읽혔다
`content: '⚔ '` — U+2694 는 기본이 **텍스트 표현**이라 얇은 선으로 그려져 작은 글씨에서
'×'로 보인다(실제 맥 크롬에서 확인). VS16(`⚔️`)로 그림 표현을 강제했다.
⚠️ **이 항목만 실기기 확인이 안 됐다** — headless 크롬에는 U+2694 이모지 커버가 없어
(🔒 는 그려지는데 ⚔️ 는 여전히 얇게 나온다) 수정 효과를 스크린샷으로 증명할 수 없었다.
이모지 폰트가 있는 기기(맥·아이패드·안드로이드)에서는 개선, 없으면 현상 유지라 되돌릴 이유는
없지만, 배포 전에 실기기에서 눈으로 한 번 봐 주면 좋겠다.

### 1-8. 새로 얻은 셈지기가 덱에 안 들어갔다 — 게이트가 볼 수 없는 사각지대
`prepScreen` 이 저장된 덱을 그대로 썼다. 1판에서 3기를 고른 아이는 **6판·8판까지 그 3기로**
싸운다 — 그 사이 4명을 새로 얻었는데도 칸 2개가 빈 채로.

실측(1판만 뛴 뒤 5판까지 진도를 넣은 저장 상태로 출전 화면 열기):
> 보유 카드 **7개** · 출전 버튼 **"출전! 3/5"** · 새로 얻은 붕붕이·먹도깨비(210)·옹기동이·붓통이 전부 미선택

아이 입장에서 "새 셈지기를 얻었다"는 보상이 전장에 아무 영향도 주지 않는다.
그리고 **밸런스 게이트는 이걸 영영 못 본다** — 프로브는 판마다 `buildDeck(보유 전부)` 로
최선의 5기를 새로 짠다. 게이트가 검증한 아이와 실제 아이가 달랐다.

빈 칸만 채우도록 고쳤다. 같은 상태에서 다시 재니 **출전! 3/5 → 5/5**.
고른 것을 빼지는 않는다. ⚠️ 다만 "일부러 5칸을 안 썼다"와 "새 셈지기를 못 봤다"를
구분하지는 못한다 — 구분하려면 저장에 '일부러 뺐음'을 기록해야 한다(스키마 변경).
2학년이 칸을 일부러 비워 둘 가능성보다 새 셈지기를 못 보고 지나칠 가능성이 훨씬 크다고 보고
채우는 쪽을 택했다.

이게 브라우저 하네스가 6·7·8판을 진 원인이었다. 프로브 표는 같은 구간에서 정답률 60%에도
100% 승리라고 말했는데, 그 간극이 정확히 덱이었다. **같은 조건으로 다시 돌린 결과:**

| 판 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 승률 |
|---|---|---|---|---|---|---|---|---|---|
| 수정 전 | 147초 | 152초 | 241초(잘림) | 73초 | 80초 | **패 2%** | **패 1%** | **패 1%** | 4/8 |
| **수정 후** | 69초 | 73초 | 64초 | 71초 | 72초 | **승 100%** | **승 100%** | **승 100%** | **8/8** |

(퍼센트 = 그 판에서 내 성 체력이 가장 낮았던 순간. 수정 후에는 8판 내내 한 대도 안 맞았다.)
판 시간도 64~79초로 균일해져 프로브 모델(ST1 @80% = 69초)과 맞는다.
1판은 보유가 3명뿐이라 여전히 "3/5" 다 — 채울 게 없으면 억지로 채우지 않는다.

**남는 질문 하나는 따로 쟀다.** 덱이 5칸을 채우고 나면 그 뒤 해금(해태장군 9판·장승 25판)은
빈 칸이 없어 여전히 자동으로 안 들어간다 — 아이가 출전 화면에서 직접 바꿔야 한다
(결과 화면이 "새 셈지기가 합류했어요!"로 알려는 준다). 그러면 **한 번도 안 바꾼 아이는
캠페인을 깰 수 있나?** 프로브에 임시로 '5판 덱 고정' 옵션을 넣어 재 봤다:

| | 일반 판 정답60% 최저 승률 | 보스(10·20·30) |
|---|---|---|
| 판마다 최적 덱 | 86% | 95 / 95 / 95% |
| **5판 덱으로 끝까지** | **86%** | **95 / 95 / 95%** |

차이 없음 — 전 게이트 통과한다(freeze 가 실제로 다른 시뮬을 도는 것도 확인: ST30 63초/19문 vs
68.5초/20문). 값비싼 유닛은 정답률 60%에서 어차피 자주 못 내기 때문이다. **그래서 덱 교체를
강제하거나 자동 교체하는 장치는 넣지 않았다.** 실험 코드는 측정 후 되돌렸다.

### 1-9. 스프라이트 알파 게이트가 CI에 없었고, 넣어도 헛돌 뻔했다
지난번 사고(배경 제거가 스프라이트 6개를 갉아먹은 채 배포됨)를 막으려고 만든
`tools/asset-check.mjs` 가 **`npm run check`(로컬)에만** 물려 있었다. 로컬에서 안 돌리면 그대로 나간다.

CI에 넣으려다 두 가지가 더 나왔다.

1. **검사 대상이 `_raw` 인데 `_raw` 는 커밋되지 않는다.** 그대로 CI에 넣었으면 "파일 0개"로
   **통과**했을 것이다 — 안 도는 것보다 **도는 척하는 쪽이 더 나쁘다.**
   실제로 배포되는 `public/assets/*.webp`(45개, 커밋됨)를 검사하도록 바꿨다.
   덤으로 **빌드 단계가 망가뜨린 경우까지** 잡힌다 — 지난 사고가 정확히 그랬다.
2. **파이썬이 이 컴퓨터의 venv 에 못 박혀 있고, 없으면 `exit 0`(통과)이었다.**
   출력만으로는 "게이트가 돌았다"와 "게이트가 자기를 껐다"가 구분되지 않았다.
   → 인터프리터를 `GUGU_PY` → 로컬 venv → `python3` 로 찾고,
   못 돌리면 **exit 1**. 일부러 건너뛰려면 `ASSET_CHECK_OPTIONAL=1` 을 명시해야 한다.

세 경로 다 실측 확인: 정상 0 / 파이썬 없음 1 / 명시적 스킵 0.

### 1-10. 밸런스 프로브가 실제보다 후한 아이를 상정하고 있었다
`DDA_STEP_ACC = 0.10` — 프로브는 막힌 아이에게 **단계당 정답률 +10%p(최대 +30%p)** 를
줬다. 실제 게임의 DDA 는 문항 *레벨*을 낮추는데, 사다리가 좁아 대부분의 아이가 이미 레벨 1
(바닥)이라 내려갈 데가 없다 → 실제 보정은 **0**.
0으로 바꿔 다시 돌려도 **전 게이트 통과**(일반 판 정답60% 최저 승률 90% → 86%,
보스 100% → 95%). 표가 실제보다 후하게 나오는 쪽이 훨씬 위험하므로 0으로 둔다.

---

## 2. 사용자 질문에 대한 실측 답

### "문제도 다양하게 나오는 거 맞지?"
**맞다.** 30판 660문항에 서로 다른 식 **257개**. 한 판 안에서 같은 식이 절반 넘게
반복되는 판은 **0개**. 실제 브라우저 플레이(8판 연속, 정답률 85%)에서도
한 판 19~23문항 중 서로 다른 식이 16~22개 — 판 안에서 거의 안 겹친다.

### "단계도 무한대고?"
**맞다.** `stageDef` 는 index 하나로 결정되며 5000판에서도 정의가 성립한다(발산·0·스폰 없음 0건).
캠페인 30판 이후는 적만 판당 ×1.05 로 세지고, 부족한 전력은 소환·승급으로 메운다.
도달 한계(승률 50% 이상) = 소환 0 → ST50, 전설 확보·승급 → ST60.

### "지금은 너무 쉬운데 나중에는 좀 어려워지기도 하지?"
**어려워진다.** 문항 유형이 3판마다 하나씩 열려 28판에 11종이 전부 열린다.

| 판 | 1 | 4 | 7 | 10 | 13 | 16 | 19 | 22 | 25 | 28 |
|---|---|---|---|---|---|---|---|---|---|---|
| 새로 열리는 갈래 | 한 자리 덧셈·뺄셈 | 받아올림 덧셈 | 곱셈구구 2~5단 | 받아내림 뺄셈 | 두 자리 ± 한 자리 | 곱셈구구 6~9단 | 나눗셈 | 두 자리 ± 두 자리 | 두 자리 × 한 자리 | 나머지 있는 나눗셈 |

1판은 한 자리 덧셈·뺄셈뿐이고, 28판은 3~4학년 과정인 나머지 있는 나눗셈이다.

---

## 3. 안 고친 것 — 사람이 정해야 하는 문제

### 3-1. ⚠️ 난이도 사다리가 좁아 목표 정답률에 아무도 도달하지 못한다
설계 목표는 L1(전투 중) 정답률 **0.85**(유창성)다. 실측:

| 아이 (그 단원 대비) | 1~10판 | 11~20판 | 21~30판 | 쓰인 레벨 |
|---|---|---|---|---|
| −200점 (아직 못 배움) | 18% | 24% | 22% | 전부 Lv1 |
| −100점 | 33% | 35% | 35% | 전부 Lv1 |
| **+0점 (막 배운 아이)** | **51%** | **50%** | **50%** | 전부 Lv1 |
| +100점 | 63% | 60% | 61% | 전부 Lv1 |
| +200점 (능숙) | 78% | 72% | 73% | Lv1 632 / Lv2 28 |

두 가지가 동시에 보인다.

1. **레벨 2~5가 사실상 안 쓰인다.** 660문항 중 Lv2 이상은 0~28개.
   유형별 5레벨이라는 조절 손잡이가 돌아가지 않는다.
   ⚠️ **브라우저 하네스에서는 반대로 보인다** — 거기선 레벨이 8판에 평균 2.19까지 오른다.
   두 모델이 다른 아이를 상정하기 때문이다. 하네스의 자동 플레이어는 **문항이 어려워져도
   정답률이 85%로 고정**이라 θ 가 천장 없이 올라가고 레벨이 따라 올라간다. 진짜 아이는
   어려워질수록 더 틀리므로 θ 가 실력에서 멈춘다 — 그래서 **실제 능력을 고정한
   `tests/learning.spec.ts` 쪽이 난이도 판정에 맞는 모델**이다. 하네스의 레벨 수치를
   "적응이 잘 되고 있다"는 증거로 읽으면 안 된다.
2. **그 단원을 막 배운 아이가 50%** — 밸런스 게이트가 가정하는 최저 정답률 **60%에도 못 미친다.**
   교육 모델과 밸런스 모델이 서로 다른 아이를 상정하고 있다.

원인은 산수로 딱 떨어진다. p=0.85 를 맞추려면 문항이 아이보다 **302 Elo 아래**여야 하는데,
`difficultyOf = baseB + (level−1)*60` 은 위쪽으로만 **240**밖에 안 펼쳐진다.
가장 쉬운 레벨 1조차 아이보다 **위**에 있으니, `pickLevel` 은 늘 레벨 1을 고를 수밖에 없다.

**한 줄 지렛대**: `src/edu/curriculum.ts`
```ts
return meta.baseB + (level - 1) * 60;    // baseB … baseB+240 (위로만)
// →
return meta.baseB + (level - 3) * 150;   // baseB−300 … baseB+300 (아이를 가운데 둔다)
```
예상 효과: 막 배운 아이 50% → 약 85%, 아직 못 배운 아이 22% → 약 60%,
레벨 1~5가 모두 쓰이고 **DDA 가 비로소 실제로 발화한다**(지금은 바닥이라 no-op).

**하지만 이건 게임 체감 난이도를 통째로 바꾼다.** 실제 정답률이 50%→85%로 오르면 셈력 수급이
크게 늘어 게임이 쉬워진다. 밸런스 게이트 전면 재실행 + "얼마나 어렵게 둘 것인가"라는
사람의 판단이 필요하다. 그래서 **오늘은 손대지 않았다.**
`tests/learning.spec.ts` 가 이 표를 매 실행 찍고, 현상을 characterization 으로 못 박아 둔다
(사다리를 고치면 그 단언이 깨져 반드시 눈에 띈다).

### 3-2. ⚠️ "L1에는 새 개념을 넣지 않는다"가 코드로는 지켜지지 않는다
`src/edu/session.ts` 첫 주석:
> 🔴 L1에는 **새 개념을 넣지 않는다.** … 새 유형 도입은 L2(관문)에서만 한다.

그런데 `quizTypesFor()` 는 `pool[pool.length-1]`, 즉 **가장 최근 열린 유형**을 항상 넣고,
전투(L1)와 관문(L2)이 **같은 `stage.quizTypes` 를 그대로 쓴다**. 28판이면 그 판에 처음
열린 '나머지 있는 나눗셈'이 전투 중에 바로 나온다. 3-1의 정답률 하락에 직접 기여한다.

선택지는 둘 중 하나이고, 어느 쪽도 그냥 고를 일이 아니다.
- **코드를 문서에 맞춘다**: L1은 관문에서 한 번이라도 만난 유형만 낸다(어떤 유형을 이미
  소개했는지 저장에 기록해야 한다 — 스키마 변경 + 진도 페이싱 변화).
- **문서를 코드에 맞춘다**: "새 유형도 전투에서 바로 낸다"로 고치고 3-1로 난이도를 잡는다.

---

### 3-3. ⚠️ 한 판 학습량이 하한(20문항)에 딱 붙어 있다 — 세지면 줄어드는 구조
덱을 고치자 판이 짧아졌다(3판 241초 → 64초). 당연한 결과지만 **부작용이 같이 온다**:
빨리 이길수록 그 판에서 푸는 문항이 준다(하네스 실측 40문항 → 19~20문항).
프로브의 G5(ST5에서 23문항)와도 대체로 맞지만, **하한 20에 여유가 거의 없다.**

이 게임에서 전력을 올리는 모든 장치(소환·승급·덱 개선·먹물)가 곧 **학습량을 깎는 방향**으로
작동한다는 뜻이다. '화면 배속'을 '내 군대 가속'으로 뒤집었던 것도 같은 문제였다
(RESEARCH.md 부록 A-6). 지금은 게이트를 통과하지만, 앞으로 전력을 올리는 무언가를 추가할 때는
**그때마다 G5 를 다시 재야 한다** — 재미를 올리는 변경이 조용히 학습량을 깎는다.

## 3-4. (막지는 않음) 첫 로딩에 배경 6장을 다 받는다
번들 자체는 가볍다 — JS 77KB(gzip 27KB), CSS 18KB. 이미지가 2.0MB, 전체 3.5MB 로
Poki 8MB 예산 안이고 진행 바도 있다. 다만 `loadAll()` 이 매니페스트 전부를 한 번에 받는데
배경 6장(148~268KB, 합 1.2MB)은 **한 판에 한 장만** 쓴다. 나머지 5장을 뒤로 미루면
첫 로딩 전송량이 60% 줄어든다. 교실 와이파이에 30명이 동시에 붙는 상황이면 의미가 있다.
지금 상태로도 배포에 문제는 없어 손대지 않았다.

## 4. 이번 검수에서 새로 생긴 도구

- `tools/qa-launch.mjs` — 실제 브라우저에서 여러 판을 연속 플레이하고 저장·중복 종료·문항
  분포를 잰다. 🔴 **드라이버를 페이지 안에 심는다**: Node↔브라우저 왕복마다 rAF 가 멈춰
  시뮬이 실제의 0.4배로 흘러 한 판도 못 끝낸다(실측).
- `tests/learning.spec.ts` — 아이 한 명을 30판 돌려 유형 개방·문항 다양성·복습 승급·
  체감 정답률·DDA 방향을 잰다. 단발 테스트로는 답할 수 없는 질문들이다.
- `window.__gugu__` 에 `hpUs`/`hpThem`/`q{key,type,level,dda}` 추가 — 화면 글자만으로는
  **유형·레벨을 복원할 수 없어** "난이도가 실제로 오르는가"를 잴 방법이 없었다.

## 5. 검수 방법에서 걸린 함정 (다음에 또 밟지 않도록)

- **배경 탭에서는 rAF 가 아예 안 돈다.** 실제 크롬 창이 가려져 있으면 `document.hidden=true`
  가 되어 시뮬 시계가 얼고 스크린샷도 타임아웃난다. 게임 버그로 오진하기 딱 좋다 —
  깊은 플레이 검수는 headless 로 한다.
- **dev 서버로 장시간 검수하지 않는다.** 소스를 고치는 순간 HMR 이 끼어들어 진행 중이던
  세션이 통째로 깨진다(실측: 8판 연속 플레이가 전부 0문항으로 무너졌다).
- **자동 플레이어를 "같은 문제는 한 번만"으로 짜면 안 된다.** 오답이 나오는 순간 그 문항에서
  멈춘다(정답률 85%로 돌렸더니 한 판에 1문항). 입력 칸이 비었는지를 신호로 쓴다.
- **자동 플레이어가 못 놀면 게임이 느린 것처럼 보인다.** 첫 판본은 셈력을 바닥까지 긁어 써서
  전선이 비었고, 3판이 195초(프로브 모델은 73초)로 나왔다 — 게임이 아니라 하네스 문제다.
  프로브의 **예비금 규칙**(비싼 걸 사도 최저가 2기분은 남긴다)을 드라이버에도 넣었다.
  프로브 주석에 "이 규칙이 없으면 돈이 많을수록 물량이 끊긴다"고 이미 적혀 있었다.

---

## 6. 최종 상태 (2026-07-31)

```
npm run check                 ✅ 167 테스트 · 전 밸런스 게이트 · 스프라이트 무결성(_raw + public/assets)
node tools/capture.mjs        ✅ 뷰포트 3종 · 콘솔 에러 0 · 검사 실패 0
npm run qa:launch             ✅ 8판 연속 8승 · 콘솔 에러 0 · 실패 0
번들                          JS 77KB(gzip 27KB) · CSS 18KB · 이미지 2.0MB · 전체 3.5MB
```

배포를 막을 것은 남아 있지 않다. §3 의 세 항목은 **사람이 정해야 하는 문제**이고,
그중 3-1(난이도 사다리)은 손대면 게임 체감이 통째로 바뀌므로 게이트 재실행이 따라온다.

---

# 2차 — 실사용 보고 대응 (2026-07-31, 같은 날 저녁)

사용자가 직접 1판을 플레이하고 네 가지를 보고했다. 전부 재현·계측했다.
계측 근거는 `t=88초` 스크린샷 + 브라우저 계측 타임라인.

## 7-1. ⚠️ "먹대포가 발사가 안 되는데?" → **적이 없어서 아무 일도 안 일어났다**

대포는 정상 충전된다(t=36초에 fill 100%, `disabled=false`, `ready` 클래스). 문제는 **쏠 대상**이었다.

```
t초 적성%  아군 적군 전선(맵1000) 대포
 24  100%   19    1    874       -
 28   98%   23    0    964       -     ← 적군 0마리
 36   88%   29    0    964      READY  ← 불은 들어오는데 때릴 게 없다
```

`fireCannon()` 이 **살아 있는 적에게만** 피해를 줬다. 전선이 적 성까지 밀고 올라가면 화면에
적이 한 마리도 없는 시간이 길어서, 그때 누르면 **충전만 사라지고 아무 일도 안 일어났다.**
정답 12개로 번 것이 아무것도 아닌 게 되면 "모든 쾌감 장치는 정답의 하류에 둔다"는 원칙이 깨진다.

→ 대포가 **적 성에도** 피해를 준다(`CANNON_CASTLE_SHARE = 0.03`, 성 체력의 3%).
전 게이트 통과, "소환 무의미"도 유지(캠페인 ST50 / 전설 ST60 격차 그대로).

## 7-2. ⚠️ "적군이 안 나오는 거니? 나오자마자 죽는 거니?" → **후자다**

적은 `x = MAP_LEN`(적 성 자리)에 스폰되는데 아군 20~54기가 `x = 964`에 눌러앉아 있어서
**스폰과 동시에 죽는다.** 게다가 `die` 이벤트를 **아무도 소비하지 않아** 죽는 표시조차 없었다
(`core.ts` 가 push 하지만 `battle.ts` 는 `hit`/`castleHit` 만 읽었다).
적이 나타나 0.3초 만에 흔적 없이 사라지니, 아이 눈에는 "안 나온다"가 맞다.

→ 쓰러진 자리에 먹 자국을 남긴다(`FieldRenderer.puff`, 420ms, `reduceMotion` 이면 생략).
밸런스는 한 줄도 안 건드린다.

## 7-3. ⚠️ "우리 애들이 많이 쌓여서" → 아군 상한 60 → **16**

한 판에 아군이 **54기**까지 쌓였다(`allyCap: 60`). 화면이 스프라이트로 덮이고 개별 유닛이
아무 의미가 없어진다. 스윕 실측(예산 고정, 상한만 변경):

| 아군 상한 | 일반60% 최저 승률 | 보스(10·20·30) | ST5 문항 | 게이트 |
|---|---|---|---|---|
| 60 (기존) | 86% | 95/95/95% | 22문 | 통과 |
| 22 | 86% | 95/95/95% | 28문 | 통과 |
| **16 (채택)** | **86%** | **95/95/95%** | **32문** | **통과** |
| 12 | 81% | 95/95/90% | 34문 | 통과 |

상한을 낮추면 게이트는 그대로인데 **한 판 학습량이 22 → 32문항으로 늘어난다**(§3-3 의
"학습량이 하한에 붙어 있다" 걱정도 같이 해소된다). 12는 승률이 눈에 띄게 떨어져 16을 택했다.
덤으로 `syncDeck` 에 **"전장이 가득"** 사유를 추가했다 — 상한이 60일 땐 닿을 일이 없었지만
16에서는 자주 닿고, 안 그러면 셈력도 충분한데 눌러도 아무 일이 없는 **7-1과 똑같은 버튼**이 된다.

## 7-4. ⚠️ "아직도 성 체력이 초기인데 너무 세" → 성 체력 계수 42000 → **25000**

1판은 **성 : 적군 체력이 9:1** 이었다. 즉 한 판의 90%가 "무저항 체력바 갈기"다.

| 판 | 1 | 5 | 10 | 20 | 30 |
|---|---|---|---|---|---|
| 성 : 적군 | **9.0:1** | 8.5:1 | 4.0:1 | 2.7:1 | 2.2:1 |

**먼저 시도했다가 버린 안 두 개** (전부 실측으로 기각):

1. **성 → 적군으로 체력 이동**(총량 유지, 9:1 → 3.3:1 / 1.7:1 / 1.0:1):
   **전부 G2 FAIL.** 일반 판 60% 최저 승률이 86% → 67% → 62% → 43% 로 무너진다.
   성은 되받아치지 않지만 적군은 때린다 — 같은 체력이라도 위험도가 전혀 다르다.
2. **후반은 고정하고 초반 물량만 증량**(ST30 예산을 고정하고 ST1 만 올림):
   역시 **전부 FAIL** — 기울기를 눕히면 중반(ST5~25)이 무너진다.

남은 지렛대는 성 체력이었다. 상한 16 기준 스윕:

| 성 체력 계수 | 1판 성 체력 | 일반60% 최저 | ST5 문항 | 게이트 |
|---|---|---|---|---|
| 34000 | 17000 | 86% | 29문 | 통과 |
| 28000 | 14000 | 86% | 26문 | 통과 |
| 23000 | 11500 | 86% | 24문 | 통과 |
| 19000 | 9500 | 86% | 22문 | **FAIL**(학습량) |

23000이 바닥이라 여유를 두고 **25000** 채택 → 1판 성 체력 **21000 → 12500 (−40%)**.

## 7-5. 난이도 사다리 — 요청받아 고쳤다 (§3-1 해소)

`difficultyOf` 를 `baseB + (level−1)*60` → `baseB + (level−3)*150` 으로 바꿔
**아이를 사다리 한가운데** 세웠다(`baseB±300`).

| 아이 (단원 대비) | 이전 | **이후** |
|---|---|---|
| −200점 (아직 못 배움) | 22% | **61%** |
| −100점 | 35% | **73%** |
| **+0점 (막 배운 아이)** | **50%** | **84%** |
| +100점 | 61% | **90%** |
| +200점 (능숙) | 73% | **93%** |

설계 목표 0.85 에 정확히 올라앉았다. 레벨 2가 실제로 쓰이기 시작했고(660문항 중 97~136개),
**DDA 도 이제 진짜로 작동한다**(0.85 → 0.93). `tests/learning.spec.ts` 의 characterization
단언 두 개를 **원래의 엄격한 단언으로 되돌렸다** — 격차가 닫혔으므로.

🔴 **이 과정에서 테스트 자체의 결함을 하나 잡았다.** 아이 실력을 `difficultyOf(t, 1)` 기준으로
잡아 놨더니, 사다리를 넓히자 **아이 실력도 같이 움직여** 정답률 표가 한 자리도 안 변했다.
기준을 `difficultyOf(t, LEVEL_MID)`(=단원 표준 난이도)로 바꿔야 변화가 보인다.
자기참조 지표는 "고쳐도 그대로"로 보이게 만든다.

## 7-6. 못 고친 것

**전선이 적 성에 닿은 뒤의 무저항 구간은 남아 있다.** 1판 기준 24초까지는 적이 나오고
전선도 움직이지만(166 → 876), 그 뒤로는 성을 깎는 시간이다.
이건 "정답률 60%인 아이가 소환 없이 캠페인 30판을 전부 깬다"는 **설계 보증의 직접적 결과**다 —
적을 실제로 위협적으로 만드는 안은 위 두 가지 모두 그 보증을 깨뜨렸다(실측).
지금 한 것은 그 구간을 **짧게(성 −40%)**, **읽히게(죽는 표시)**, **덜 어수선하게(상한 16)**
만든 것이다. 구조를 바꾸려면 그 보증부터 다시 정해야 한다.

## 7-7. 2차 수정 후 최종 검증

```
npm run check      ✅ 167 테스트 · 전 밸런스 게이트 · 스프라이트 무결성
                      일반 판 정답60% 최저 승률 86% · 보스 95/95/95%
                      ST5 문항 25문 · 소환 무의미 유지(캠페인 ST50 / 전설 ST60)
tools/capture.mjs  ✅ 뷰포트 3종 · 콘솔 에러 0 · 검사 실패 0
npm run qa:launch  ✅ 8판 연속 8승 · 콘솔 에러 0 · 실패 0
```

브라우저 8판 연속(정답률 85%, 문항당 3.2초):

| 판 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 시간(초) | 70 | 137 | 71 | 72 | 79 | 87 | 88 | 89 |
| 문항 | 19 | 38 | 20 | 20 | 20 | 25 | 21 | 26 |
| 평균 레벨 | 1.00 | 1.03 | **1.50** | **1.55** | **1.50** | **1.56** | **1.43** | 1.23 |

**평균 레벨이 실제 플레이에서도 오른다** — 사다리 교정 전에는 30판을 돌려도 1.00 에 붙어 있었다.
관측된 DDA 단계도 0,1 로 **실제로 발화한다**. 2판(137초/38문)은 소환 운이 나빴던 이상치이고
G1(55~240초) 안이다.

---

# 8. 3차: "적군이 성 안에서 나오다가 맞아서 안 보이는 거 아니냐" (2026-08-01)

사용자 질문 그대로: *"아군이 성을 공격하는 거랑 … 적군이 성 안에서 중간에서 나오면 …
성보다 적군이 더 앞으로 나오면 적군을 때리면 적군이 보이지 않아?"*

**맞았다.** 추측이 아니라 좌표 문제였고, 세 가지가 정확히 겹쳐 있었다.

## 8-1. 원인 — 성 그림이 전투 구역을 덮고 있었다

렌더러가 성 스프라이트를 월드 끝점에 **중심 정렬**해 그렸다(`x = cx - w/2`).
성 스프라이트는 월드 단위로 **195칸(맵 전체의 19.5%)** 이라, 그 안쪽 절반이 전투 구역을 통째로 덮었다.

900×480 캔버스 실측:

| | 월드 좌표 | 어디에 그려지나 |
|---|---|---|
| 적 성 스프라이트 | **902.7 ~ 1097.3** | — |
| 적 스폰 지점 | **1000** | 성 그림 **한가운데** |
| 근접 아군 정지점 | **963** | 역시 성 그림 **안쪽** |

아군이 963에 서는 이유: 성 타격 판정이 `|1000 − x| ≤ 사거리`인데 근접 사거리가 38이라
963에서 멈춘다. 그래서 아군도 적도 검은 성 그림 위에 겹쳐 그려졌다.
사용자 스크린샷에서 짚신이가 성에 파묻혀 보이던 게 이것이다.

## 8-2. 원인 — 적은 태어난 자리에서 한 픽셀도 못 움직이고 죽는다

`combat()` 은 사거리 안에 유닛이 있으면 이동 분기(`else if (u.spd > 0)`)를 아예 타지 않는다.
아군이 963에 서 있으면 적(1000)과 거리 37 → 스폰 즉시 교전 상태가 된다.

ST1 실측(`npx vite-node tools/enemy-visibility-probe.ts`):

| 등장 | 생존 | 최전진 위치 | 성에서 나온 거리 |
|---|---|---|---|
| 3.0초 | 10.60초 | x=705 | 295px |
| 11.9초 | 5.90초 | x=843 | 157px |
| 20.7초 | 1.53초 | **x=1000** | **0px** |
| 29.6초 | 0.87초 | **x=1000** | **0px** |
| 38.4초 | 0.83초 | **x=1000** | **0px** |
| 47.3초 | 0.67초 | **x=1000** | **0px** |

즉 **3번째 적부터는 성문 앞에서 1초도 못 버티고, 그 1초가 전부 성 그림에 가려 있었다.**
"안 나오는 거니, 나오자마자 죽는 거니"는 — 나오자마자 죽는데 그게 안 보인 것이다.

## 8-3. 고친 것 (전부 렌더러 — 밸런스 수치 0 변경)

1. **성을 전장 밖에 세운다.** 성의 **안쪽 면**이 월드 끝점이 되도록 바깥으로 밀었다
   (`castleBox()`). 성문 앞이 전부 빈 땅이라 교전이 그대로 보인다.
2. **성 크기를 가로폭도 보고 정한다.** 예전엔 높이만 봐서(`min(160, h*0.5)`) 좁고 높은
   화면에서 성 하나가 가로폭의 37%를 먹었다. 이제 `min(160, h*0.5, w*0.18)`.
3. **성문 등장 표시**(`spawn` 이벤트 → `gate()`). 적이 나오는 순간 성문에서 빛이 번지고
   발밑에 먼지가 인다. 죽는 자리 표시(`puff`)와 짝이다 — 나오는 순간과 쓰러지는 순간이
   둘 다 보여야 "나왔다가 맞고 쓰러졌다"가 한 장면으로 읽힌다.
   `spawn` 은 그리기 전용 이벤트라 시뮬 수치에 영향이 없다.

**실측 확인**
- 성문 빛: 성문 지점 픽셀의 R−B 값이 기준 19 → **114**(6배). 실제로 그려진다.
- 밸런스 불변: `일반 판 정답60% 최저 승률 86%` — 수정 전과 **같은 값**.
- 게이트: 202 테스트 전건 통과, 전 밸런스 게이트 통과, 스프라이트 위반 0건.
- 화면: 데스크톱 1280×800 / 태블릿 834×1112 / 휴대폰 가로 844×390 — 콘솔 에러 0건,
  세 곳 모두 적이 성 밖 빈 땅에서 교전하는 것을 눈으로 확인.

## 8-4. 회귀 방지

`tests/field-geometry.spec.ts` — 7개 화면 크기 × 5개 불변식(35 테스트).
성 그림의 안쪽 모서리를 **월드 좌표로 되돌려** 전투 구역을 침범하지 않는지 잰다.

🔴 공식을 테스트에 복붙하지 않았다. 렌더러가 **실제로 쓰는** `castleBox()` 를 호출한다.
   (공식을 옮겨 적으면 아무것도 검증하지 못한다 — §5에서 이미 한 번 밟은 함정이다.)
   되돌려 확인: `castleBox` 를 옛 중심정렬로 바꾸면 **14건 FAIL**
   (`expected 880 to be greater than or equal to 999.5`).

## 8-5. 남는 것 — 적 물량 자체는 여전히 적다

ST1은 63초에 적 7마리, 스폰 간격 8.8초다. 이건 그림 문제가 아니라 예산 문제다.
"정답률 60% 아이가 소환 없이 캠페인을 통과한다"(G2)는 보장이 적 총 체력을 묶고 있고,
**적을 잘게 쪼개 수를 늘리는 안은 못 쓴다** — `hpMul` 은 체력만 나누고 공격력은 그대로라
마릿수를 2배로 하면 들어오는 피해가 2배가 된다(G2 붕괴). §7-4에서 성→적군 체력 이전과
초반 물량 증가를 각각 스윕해 전부 FAIL 한 것과 같은 벽이다.

지금 상태에서 화면에 적이 있는 시간은 표본 기준 **37% → 75~89%** 로 올라갔다(전선 위치에
따라 다름). 이건 물량이 늘어서가 아니라 **보이게 돼서** 그렇다. 더 늘리려면 G2 보장 수치
자체를 사람이 다시 정해야 한다.

---

# 9. 4차: "너무 쉬워서 노잼" — 적응형 난이도 (2026-08-01)

사용자 보고: *"근데 지금은 너무 쉬운 것 같아.. 게임이 그래서 약간 노잼이야."*

## 9-1. 진단 — "쉽다"가 아니라 **"긴장이 0"** 이었다

승률 게이트는 전부 통과인데 재미가 없었다. 승률은 결과고 재미는 과정이라, 과정을 쟀다
(`tools/tension-probe.mjs`, 21시드 × 9판 × 정답률 4구간).

| 정답률 | 진 판 | 우리 성이 맞은 판 | 아슬아슬하게 이긴 판 |
|---|---|---|---|
| 40% | 35% | 38% | 3% |
| 60% | 4% | 7% | 1% |
| **80%** | **0%** | **0%** | **0%** |
| **95%** | **0%** | **0%** | **0%** |

**잘하는 아이 378판 중 우리 성이 한 대라도 맞은 판이 0개.** 이길지 질지가 아니라 구경이었다.

## 9-2. 손잡이가 하나도 안 들었다 — 구조 문제였다

| 시도 | 결과 |
|---|---|
| 적 체력 ×1.6 / ×2.2 / ×3 / **×4** | 정답80%+ 승률 **100% 그대로**, 성 피격 0% |
| 스폰 간격 ×0.5 / ×0.3 / ×0.2 / **×0.1**(몰려오게) | 성 피격 0% → 22%, 승률은 거의 그대로 |
| 적 공격력 ×3 | 정답40% 아이가 89% 패배(안전망 붕괴)인데 정답95% 아이는 **0% 패배** |
| 적 공격력 **×7** | 정답95% 아이 여전히 **0% 패배** |

원인: **되먹임에 반대 힘이 없다.** 잘 맞힐수록 셈력↑ → 유닛↑(상한 16) → 신바람↑ →
더 빨리 죽임 → 더 여유롭게 답함. 그리고 상비군 16기가 길목을 완전히 막아서
적 체력·물량을 아무리 올려도 **성문 앞에서 16대1로 죽을 뿐**이었다.
→ 숫자 하나로는 못하는 아이와 잘하는 아이를 동시에 못 맞춘다.

⚠️ **요약통계가 분포를 가린 사고**: 처음엔 21판의 *중앙값* 성 체력을 봤다. 결과가 0% 아니면
100%로 갈리는 분포라 중앙값이 그 사이를 통째로 지웠고, 어떤 배율에서도 "아슬아슬 0건"이
나와 손잡이가 안 듣는 것처럼 보였다. **판 단위로 세고 나서야** 무엇이 듣는지가 보였다.

## 9-3. 고친 것 — 적응형 난이도 단계 0~8 (`src/sim/tier.ts`)

사용자 선택: 돌파형 적 + 광역 공격 + 적 공격력 상향, 안전망은 **적응형 자동 조절**,
문항은 **전투 중 목표 정답률 하향**. 넷을 **단계 하나**에 묶었다.

| 단계 | 0 | 2 | 4 | 6 | 8 |
|---|---|---|---|---|---|
| 적 공격력 | ×1.0 | ×2.6 | ×4.8 | ×7.8 | ×11.6 |
| 돌파형 비율 | 0 | 25% | 42% | 58% | 70% |
| 광역 반경 | 0 | 70 | 100 | 130 | 160 |
| 문항 목표 정답률 | 0.85 | 0.82 | 0.79 | 0.77 | 0.77 |

- **돌파형** = 전선과 교전하지 않고 지나쳐 우리 성으로 직행. 상비군이 길목을 막는다는
  전제 자체를 깬다 — 원거리·빠른 셈지기를 뽑을 이유(=덱의 의미)가 여기서 처음 생긴다.
  발밑 화살표 + 머리 위 삼각형으로 표시한다(색만으로 구분하지 않는다).
- **광역** = 뭉친 아군을 쓸어버린다. "16기 쌓고 방치"를 정면으로 무력화.
  느리고 단단한 적·수문장에게만 준다(빠른 잡몹까지 광역이면 전선이 즉사한다).
- **조정 규칙** = 여유롭게 **2판 연속** 이기면 +1, **한 판만 져도** 즉시 −1. 비대칭이 의도다.
- 결과 화면이 바뀐 사실을 알려 준다(조용히 바뀌면 "왜 갑자기 어렵지"만 남는다).

### 🔴 안전망은 설계상 보장된다

**0단계 = 종전 밸런스와 완전히 동일**(배율 1.0 · 돌파 0 · 광역 0 · 목표 0.85)하게 만들었다.
적응 루프는 **이긴 아이만** 올리므로 어려워하는 아이는 0단계를 벗어나지 못한다 →
G2가 검사하는 그 밸런스를 그대로 만난다. `tests/tier.spec.ts` 가 0단계에 조미료가
새어 들어가지 않는지 감시한다.

## 9-4. 실측 — 적응 루프 30판 연속 (21명씩)

| 아이 실력 | 머문 단계 | 패배율 | 성 피격률 |
|---|---|---|---|
| 정답 40% | **0.0** | 42% | 45% |
| 정답 60% | **0.1** | 7% | 9% |
| 정답 75% | 1.6 | 14% | 20% |
| 정답 85% | 3.4 | 15% | 27% |
| 정답 95% | 5.2 | 4% | 14% |

**성 피격률 0% → 9~27%.** 못하는 아이는 21명 전원 0단계에 그대로 남는다.

문항도 같이 올라간다(30판 종단, 같은 아이):

| | 문항 난이도 | 평균 레벨 | 체감 정답률 |
|---|---|---|---|
| 0단계 | 899 | 1.15 | 84% |
| 6단계 | 1012 | **1.94** | 72% |

그 단원을 확실히 넘어선 아이는 6단계에서 **레벨 3+ 문항이 47%** 나온다
(예전에는 30판 내내 레벨 3~5가 **0개**였다).

⚠️ 목표 정답률 바닥을 0.72로 뒀다가 되돌렸다 — 체감 정답률이 **62%** 로 떨어졌는데,
열 문제 중 넷을 틀리는 건 저학년에게 도전이 아니라 벌이다. 0.77(체감 ~73%)에서 멈추고,
아주 잘하는 아이에게는 **전투 쪽만** 더 올린다(7·8단계).

## 9-5. 회귀 방지

- `tests/tier.spec.ts` (19) — 0단계 무개입 / 표 단조성 / 조정 규칙 / 돌파·광역 실동작 /
  프로브 표 일치. 
  ⚠️ 처음 쓴 "돌파형이 전선을 넘는다" 테스트는 **아군을 소환하지 않아** `allyFront`가
  `-Infinity`였다 — 기능이 아니라 자기 설정을 재고 있었다. 소환을 넣고서야 관측이 됐다.
  같은 이유로 반대편 테스트에도 `sawAlly` 단언을 넣어 빈 검사를 막았다.
- `tools/balance-probe.mjs` **G8 재미 게이트** — 승률 게이트가 못 잡는 것을 잰다.
  ⚠️ 처음엔 "최고 단계가 정답80% 아이에게 가혹한가"를 봤는데, 최고 단계는 **아무도 머물지
  않는 천장**이다(그 조건 패배율 83% → '벽'으로 FAIL). 재야 할 건 **적응 루프가 아이를
  어디에 데려다 놓는가**다 — 30판을 이어서 돌려 아이가 실제로 겪은 것을 센다.
- 세이브 v4 (`challenge.tier`) — 없으면 0. 기존 저장을 갑자기 어렵게 만들지 않는다.

## 9-6. 의도한 상충 — 아군 상한과 돌파형

아군 상한이 16이라, 전선에 16기를 다 채워 둔 상태에서는 **돌파형이 새어 들어와도 새로
소환할 수 없다**(덱 카드에 "전장이 가득"이 뜬다). 이건 버그가 아니라 대가다 —
"다 밀어 넣고 방치"가 지배 전략이 아니게 만드는 게 이번 변경의 목적이었다.
돌파형의 대응 수단은 두 가지이고 둘 다 덱에서 온다: **원거리 셈지기**(지나가는 놈을 쏜다)와
**여유분을 남기는 운용**이다.

⚠️ 다만 아이 눈에는 "위급한데 버튼이 안 눌린다"로 보일 수 있다. 실측상 승률은 유지되지만
(정답 75~95% 아이 패배율 4~15%), 실제 아이가 답답해하면 **상한을 단계와 함께 올리는 것**이
먼저 볼 손잡이다. 지금은 올리지 않았다 — 올리면 "쌓아 두기"가 다시 최적이 될 수 있어서다.

## 9-7. 교차검증(FULL) 결과 — 세 계열 독립 리뷰

세이브 스키마 마이그레이션(v3→v4)이 포함돼 FULL로 돌렸다.
Tier-0 게이트 전항목 PASS 후 codex(GPT)·Gemini·code-reviewer(Claude)를 병렬로 붙였다.
셋 다 REQUEST_CHANGES. **아래는 전부 내가 직접 재현한 것만 적는다.**

### 고친 것

1. **게이트가 오염된 실행에도 초록불을 냈다** (codex CRITICAL — 재현됨)
   `GUGU_SWEEP_ATK=1.01 node tools/balance-probe.mjs` → 상수가 바뀐 상태로
   **"✅ 전 게이트 통과" + exit 0**. 이 게이트가 증명하는 건 "0단계 = 종전 밸런스"인데,
   노브가 켜진 실행의 통과는 그 증명이 아니다. 경고만 찍고 통과시키면 CI에서 조용히 초록불이 된다.
   → `SWEEPING`이면 **exit 2**로 끊는다. 정상 실행은 그대로 exit 0.

2. **공허한 단언 (세 번째)** — `tests/tier.spec.ts` "돌파형은 고정형에 안 걸린다"
   두 단계로 헛돌고 있었다.
   - ① 아군을 안 세우면 성이 **t=25.9초**에 무너지는데 보스 스폰은 **t=35초** — 고정형 관측 **0회**.
   - ② 아군을 세워 등장시켜도, 보스는 자기 스폰 ID의 **0번**이고 Bresenham은 비율<1에서
     0번을 절대 고르지 않는다 → `e.spd > 0` 가드를 **지워도 통과**했다(실측).
   → 고정형 `cap`을 늘려 **1번 순번**(비율 0.7에서 반드시 뽑히는 자리)을 만들어, 가드가
     유일한 방어선인 판을 구성했다. 가드 제거 시 **FAIL** 확인.

3. **프로브 미러 누락 — `enemyFloor`** (codex WARNING — 재현됨)
   실제 코어는 일반 적에게 전선 하한(`allyFront - 60`)을 거는데 프로브엔 그 로직이 아예 없었다
   (내 변경이 만든 게 아니라 원래 없었다. 다만 내가 추가한 G8이 이 프로브를 쓴다).
   → 미러 추가. **게이트 수치 변화 0** — 계측해 보니 그 하한이 실제로 걸린 횟수도 **0회**였다
   (적은 사거리에 들면 대상을 잡아 멈추므로 60px 여유를 넘지 못한다). 소스 격차는 실재했고
   거동 영향은 없었다.

4. **`nextTier` 삼중 복제** (code-reviewer WARNING — 확인됨)
   `src/sim/tier.ts` · `balance-probe.mjs` 인라인 · `adaptive-probe.mjs`에 각각 있었고,
   parity 테스트는 **표 값 4개만** 대조했다. 임계값(0.85 / 0.7)이나 연승 조건이 갈라져도
   초록불이라, G8이 구버전 규칙으로 돌며 "안전망이 지켜진다"고 말할 수 있었다.
   → 프로브 사본을 `probe-model.mjs` 하나로 합치고, **출력 격자 792케이스**를 대조하는
     parity 테스트를 넣었다. 프로브 임계값을 0.85→0.80으로 틀어 **FAIL** 확인.

5. **NaN 누수** (내가 찾음) — `Battle`이 자체 클램프를 써서 `tier=NaN`을 그대로 들고 있었다.
   `Math.max(0, Math.min(N, Math.floor(NaN)))`는 0이 아니라 NaN이다. 전투 수치는 내부에서
   다시 잘려 무해했지만, 결과 화면의 `nextTier !== tier` 비교가 **항상 참**이 되어 난이도가
   안 바뀐 판에도 안내가 뜬다. → 공유 `clampTier` 하나만 쓴다.

6. **`challenge.streak` 상한 한 칸 넉넉** (codex NIT) — `nextTier`가 만드는 대기 streak 최댓값은
   `EASY_STREAK_TO_RAISE - 1`이다. 상한을 그 값으로 좁혔다(손상 저장의 streak=2 → 1승 즉시 승급 방지).

### 걸렀거나 판단을 미룬 것

- **"너무 쉬웠죠?"가 아슬아슬하게 이긴 아이에게 조롱으로 읽힌다** (Gemini) — **전제 오류로 기각**.
  승급은 `castleLeft >= 0.85 && accuracy >= 0.7`인 **여유 승리 2연속**에서만 발동하므로
  아슬아슬한 승리에는 이 문구가 뜨지 않는다. (어투 자체에 대한 판단은 별개로 남겨 둔다.)
- **`nextTier()`를 `store.update()` 밖에서 계산해 경쟁 상태** — codex가 스스로 부정했다.
  단일 탭에선 `await`가 없고 `finished` 잠금이 있어 문제없다. **여러 탭**을 동시에 열면
  세이브 전체를 덮어쓰는 기존 구조상 lost-update가 나지만, 이번 변경이 만든 게 아니고
  콜백 안으로 옮겨도 해결되지 않는다 — 저장 구조 자체의 별건이다.
- **일부러 져서 난이도를 낮추는 경로** (Gemini) — 보상 미지급이라는 대가가 이미 있고,
  쉬워지고 싶은 아이가 쉬워지는 건 이 시스템의 의도이기도 하다. 조치 안 함.
- **무승부(시간초과)가 완패와 같은 -1** (Gemini) — 로직은 안전하다는 데 동의.
  다 못 끝낸 판을 쉬워지게 하는 건 맞는 방향이고, 문구도 어긋나지 않는다. 조치 안 함.

### 남겨 둔 판단 — 난이도 카드가 자주 뜬다 (실측)

Gemini가 지적한 진동을 재 봤다. 승급은 "여유 2연승", 강등은 "1패 즉시"라 비대칭이어서
(승,승,패)가 반복되면 단계가 계속 오르내린다.

| 아이 실력 | 30판 중 난이도 변경 | 그중 방향이 뒤집힌 횟수 |
|---|---|---|
| 정답 40% | 0.0회 | 0.0회 |
| 정답 60% | 1.2회 | 0.7회 |
| 정답 75% | 11.0회 | 6.5회 |
| 정답 85% | **13.9회** | 6.1회 |
| 정답 95% | 10.4회 | 2.3회 |

잘하는 아이는 **두 판에 한 번꼴로** "세졌어요 / 약해졌어요" 카드를 본다.
단계가 ±1로 흔들리는 것 자체는 적응 루프가 경계를 찾는 정상 동작이고, 안전망(1패 즉시 강등)은
일부러 그렇게 만든 것이라 **비대칭을 없애면 막힌 아이를 붙잡아 두게 된다.**
그래서 규칙은 건드리지 않았다. 손을 댄다면 규칙이 아니라 **안내 빈도**(되돌이일 땐 카드를 접기)가
먼저 볼 손잡이다. 아이가 실제로 어떻게 읽는지는 교실에서 봐야 판단할 수 있다.

# 10. "적이 아예 안 나온다" — 두 번 오진한 버그

실사용자(교사)가 같은 증상을 **두 번** 보고했다. 두 번 다 렌더 문제로 봤고, 두 번 다 틀렸다.

## 10-1. 첫 번째 오진 — 성 그림 (8절, 실제로 있던 문제)

성 스프라이트가 전투 구역을 덮고 있었다. 이건 실재했고 고쳤다(커밋 `fe8c6a0`).
그런데 그걸 고친 뒤에도 사용자는 같은 말을 다시 했다. **증상이 남았다는 건 원인이 더 있었다는 뜻이다.**

## 10-2. 진짜 원인 ① — 적이 나올 자리가 없다

적은 월드 1000(성문)에서 나오고, 근접 아군은 사거리 38이라 962에 선다.
화면상 **23px**. 스프라이트 폭은 60~90px이다 — 적이 나오는 자리가 아군 더미 안쪽이다.

전선이 적 성에 닿은 뒤 스폰된 적을 전수 추적한 결과:

| | ST1 | ST5 | ST12 |
|---|---|---|---|
| 그 뒤 죽은 적 | 6기 | 11기 | 14기 |
| 평균 생존 | 0.88초 | 0.75초 | 1.62초 |
| **한 픽셀도 못 움직인 적** | **6/6** | **11/11** | **14/14** |

**31기 전원이 한 발짝도 못 떼고 죽었다.** 렌더 문제가 아니라 시뮬 문제였다.

→ `ALLY_CEIL_GAP = 90`. 적의 `enemyFloor`(아군 전선을 60 넘게 못 파고든다)와 **대칭인 상한**이다.
통로 끝에 선 아군은 사거리가 짧아도 성을 때린다(`atCeil`) — 이 예외가 없으면 근접 셈지기가
성에 못 닿아 판이 영원히 안 끝난다. `tests/spawn-corridor.spec.ts`가 그 조건을 직접 검사한다.

## 10-3. 진짜 원인 ② — 적 총량이 판 중간에 마른다

사용자가 "성 앞에 모이면"이라고 한 시각과 스폰이 마르는 시각이 겹친다.

| 판 | 적 총량 | 마지막 스폰 | 판 끝 | **적이 한 마리도 안 나오는 시간** |
|---|---|---|---|---|
| ST1 | 9기 | 65초 | 67초 | 2초 |
| ST3 | 16기 | 51초 | 79초 | 28초 (35%) |
| ST5 | 18기 | 60초 | 87초 | 27초 (31%) |
| ST12 | 22기 | 56초 | 121초 | **65초 (54%)** |
| ST20 | 23기 | 60초 | 119초 | 59초 (50%) |

12판은 **판의 절반 이상이 무저항 구간**이었다. 원인은 두 시계가 안 맞는 것이다 —
스폰 상한은 `적 HP 예산 ÷ 마리당 체력`으로 정해지는데, 판이 끝나는 조건은 **적 성 체력 0**이다.

⚠️ 이게 "너무 쉽다"의 진짜 원인이기도 했다. 9절에서 적 체력 ×4, 스폰 몰아치기 ×10을 걸어도
효과가 없었던 이유가 여기 있다 — **총량을 키워도 다 쓰고 나면 똑같이 빈다.**

→ 예비대(`RESERVE_SHARE = 0.25`, `RESERVE_COVER = 0.85`): 총량 중 뒤쪽 25%는 시간이 아니라
**적 성 진행도**에 묶어 배출.
`stages.ts`가 명시한 "무한 반복 = 느릴수록 불리해지는 죽음의 나선" 제약을 지키려면
**총량을 늘리면 안 된다.** 예비대는 언제 나오는지만 바꾼다.

### 튜닝 이력 — 재배치만으로는 반드시 쉬워진다

처음엔 `RESERVE_SHARE = 0.5`, 배출 곡선을 진행도 1.0까지로 뒀다. 결과는 **게이트 3건 실패**,
그것도 전부 "너무 쉽다" 쪽이었다(찍기 통과 판 8→14개, 무한구간 도달 ST50→ST60).

이유는 두 가지다.
- 진행도 1.0에서 풀리는 예비대는 **성이 무너지는 순간** 나와 사실상 안 싸운다 → `RESERVE_COVER = 0.8`.
- 더 근본적으로, **미룬 적은 플레이어가 가장 강할 때 도착한다.** 총량이 고정이면
  재배치는 반드시 난이도를 낮춘다. 그래서 비율을 0.5 → 0.25로 낮춰 균형점을 찾았다.

두 변경을 따로 켜 봐서 원인을 분리했다:

| 설정 | 정답40% 패배율 | 정답60% 최저승률 | 찍기 통과 판 | 게이트 |
|---|---|---|---|---|
| 원본 | 42% | 86% | 8개 | 통과 |
| 통로만 | 55% | 86% | 6개 | 통과 |
| 예비대만(0.5) | 12% | 95% | 16개 | **3건 실패** |
| **통로+예비대(0.25)** | 46% | 86% | — | **통과** |

## 10-4. 결과

| | 이전 | 이후 |
|---|---|---|
| ST12 빈 시간 | 65초 (판의 54%) | 42초 (17%) |
| ST20 빈 시간 | 59초 (50%) | 30초 (15%) |
| ST3 빈 시간 | 28초 (35%) | 3초 (3%) |
| 적 평균 생존 | 0.75~1.62초 | 3.6~12.1초 |
| **스폰 후 움직인 적** | **0/31기** | **전원** |

## 10-5. 교훈

**같은 증상을 사용자가 두 번 말하면, 첫 번째 수정이 원인의 일부만 짚은 것이다.**
"고쳤는데 또 그러네"를 사용자 착각으로 돌리지 않고 다시 실측한 것이 이 절의 전부다.
그리고 두 원인 모두 **화면이 아니라 시뮬에** 있었다 — 증상이 시각적이라고 원인도 시각적인 건 아니다.

# 11. 모바일 가로 레이아웃 — 숫자패드가 화면의 45%

실사용자: "지금 캐릭터를 드래그 하게 하네… 아래가 너무 찬다, 특히 모바일".

이 게임은 **가로 전용**이다(세로에서는 회전 안내가 뜬다). 그래서 세로에서 잰 수치는 의미가 없다 —
처음에 세로로 재고 원인을 잘못 짚었다. 가로(844×390, iPhone 14)에서 다시 쟀다:

| 칸 | 이전 | 필요 | 이후 |
|---|---|---|---|
| 셈지기 덱 | **188px** | 286px (5장) | **286px ✅** |
| 먹대포 | 52px | | 52px |
| 문제 | 194px | | 143px |
| **숫자패드** | **379px (45%)** | | **319px** |

원인은 두 겹이었다.
- `.control` 그리드에서 **덱만 최소폭 0**이라, 모자란 폭을 덱이 혼자 다 뒤집어쓴다.
- 패드 폭은 버튼 최소폭 × 6열로 정해지는데, 그 값이 `7vw`(844px에서 59px)로 박혀 있었다.
  ⚠️ 기본 규칙(`8.4vw`)이 아니라 **`max-height: 430px` 미디어 쿼리 안의 값**이 실제 적용값이다.
  기본 규칙만 고치고 측정했다가 아무것도 안 바뀌는 걸 한 번 겪었다.

44px는 저학년 터치 타겟 하한이라 그 아래로는 내리지 않았다. 대신 좁은 가로(≤820px)에서는
카드를 54→48px로 줄여 5장이 다 들어가게 했다.

측정 결과(전부 5장 전부 보임): 데스크톱 1280×720 · 태블릿 1024×768 · 아이패드 1194×834 ·
iPhone 14 가로 844×390 · 안드로이드 800×360 · 좁은 가로 740×360 · 초저높이 900×310.
⚠️ **568×320(2016년 SE 가로)만 여전히 잘린다** — 패드 289 + 대포 52 + 문제 112 를 빼면
덱에 85px밖에 안 남는다. 그 폭에 5장을 넣으려면 카드가 터치 타겟 아래로 내려가야 해서 포기했다.

# 12. 없던 연출 — 원거리·먹대포·뽑기

실사용자: "먹대포 이펙트는 여전히 없음", "원거리 공격에서 뭐 날아가는 것도 안 보이는데",
"뽑기 할 때 이펙트가 너무 없어서 심심함", "똑같은 애가 나오네??".

전부 사실이었다. 코드를 확인한 결과:

- **원거리**: 사거리는 실제로 작동한다(청룡 250 vs 짚신이 38). 그런데 시뮬이 즉시 체력을 깎고
  렌더러에 투사체를 그리는 코드가 **아예 없었다**. 제자리에서 0.18초 흔들릴 뿐이었다.
  → hit 이벤트에 `from`(쏜 위치)을 실어 렌더러가 포물선으로 날아가는 먹덩이를 그린다.
  **그리기 전용이라 밸런스에 영향이 없다** — 맞은 결과는 이미 시뮬에서 났다.
- **먹 대포**: 전용 연출이 없었다. 화면 흔들림 4px + 기존 피격음이 전부였다.
  정답 12개를 모아 쏘는 이 게임 최대의 보상인데 화면에서 아무 일도 안 일어났다.
  → 우리 성에서 먹물 파도가 금빛 앞머리를 달고 전장을 쓸고 간다(620ms).
- **뽑기 중복**: `조각 +6` 태그는 있었지만 **그게 뭘 위한 건지, 얼마나 남았는지**가 없었다.
  아이 눈엔 그냥 꽝이다. → `조각 +6` + `5레벨까지 3개` / `5레벨로 올릴 수 있어요!`.
  ⚠️ 겸사겸사 버그 하나: 결과 카드가 레벨을 **항상 Lv.1로 하드코딩**하고 있었다(4레벨까지
  키운 셈지기를 뽑아도 Lv.1로 보였다).
- **뽑기 연출**: 전부 220ms 간격으로 똑같이 넘어가 전설도 잡몹처럼 지나갔다.
  → 유니크 이상은 620ms 뜸을 들이고 금빛 테가 퍼진다.

검수는 실제 브라우저에서 숫자로 확인했다(화면을 눈으로 세지 않는다):
투사체 15발 · 먹대포 1발 · 유니크 이상 연출 4장 · 조각 안내 표시 확인.
그러려고 렌더러에 `shotsFired`/`sweeps` 카운터를 두고 `window.__gugu__.fx`로 노출했다.


# 13. 교차검증(FULL) 2회차 — 세 계열이 각각 다른 걸 잡았다

라우터가 FULL을 지시했다(`crit=1`). codex·Gemini·code-reviewer 병렬.
판정: **codex REQUEST_CHANGES · Gemini 로직 APPROVE/카피 REQUEST_CHANGES · Claude APPROVE**.
CRITICAL 0건. 아래는 **전부 내가 직접 재현한 것만** 적는다.

## 13-1. 고친 것

**① 만렙 중복이 없는 레벨을 안내한다** (Gemini·codex 공통, 재현됨)
`canUpgrade`는 만렙과 조각 부족을 **구분하지 않고** 둘 다 `false`를 준다. 그걸 "부족"으로 읽으면
보통 등급(최대 5레벨·뽑힐 확률 55%)이 5레벨에 닿는 순간부터 영영 이렇게 뜬다:

```
1번째 중복 → "6레벨까지 5개"
2번째 중복 → "6레벨까지 1개"
3번째 중복 → "6레벨까지 1개"   ← 6레벨은 존재하지 않는다
```

가장 흔한 등급에서 가장 먼저 터지는 경로였다. → `이미 최고 레벨이에요`.

**② 10연에서 같은 셈지기가 두 번 나오면 첫 카드가 두 장 몫을 보여 준다** (Gemini 추가 발견)
결과 화면이 저장을 한 번만 읽어서, 먼저 열리는 카드가 이미 배치 전체가 반영된 최종 조각 수를
표시했다("아직 모자란데 벌써 올릴 수 있대"). → 뽑기 **전** 상태를 남겨 두고 열리는 순서대로 다시 쌓는다.

**③ 예비대가 cap ≤ 3 웨이브에는 아예 안 걸렸다** (codex·Claude 공통, 재현됨)
`onTime = ceil(cap × 0.75)`는 cap 1·2·3에서 `onTime === cap`이 되어 **예비대가 0기**다.
12판은 웨이브 8개 중 4개가 이 사각지대였다 — 주석은 "뒤쪽 25%"라는데 실제로는 안 걸렸다.
**주석이 코드보다 많은 것을 주장하고 있었다.** → cap 1(수문장)만 예외로 두고 2 이상은 최소 1기 보장.

**④ 진행도를 넘겨도 다음 폴링까지 기다린다** (codex, 재현됨)
차단될 때 `spawnNext = t + s.every`로 미뤄서, 진행도를 넘긴 뒤에도 `every`초를 더 기다렸다.
그 사이 판이 끝나면 **영영 안 나온다**(실측: COVER 0.9에서 16기 중 2기 미출전).
→ 미루지 않고 매 프레임 다시 본다. 이게 앞선 튜닝이 헛돌던 진짜 이유였다.

**⑤ `sweeps` 카운터가 안 그려도 증가한다** (Claude)
`shotsFired`는 `reduceMotion`이면 안 세는데 `sweeps`는 셌다. 검수 하네스가 "연출 났다"로 오판할 수 있다.

**⑥ 아주 좁은 가로(667×375, 아이폰 SE 2·3세대)는 여전히 잘린다** (codex, 계산으로 지적 → 실측 확인)
→ 700px 미만에서는 숫자패드를 4열 3줄로 접는다. 측정: 232/232px **전부 보임**.
568×320(2016년 SE)만 21px 잘림으로 남았다(이전 209px).

**⑦ 미러 테스트가 예비대 거동을 못 잡는다** (codex)
프로브에서 예비대 조건문을 **통째로 지워도 통과**했다 — 개수만 보고 시점을 안 봤다.
→ 스폰 시점의 성 진행도를 대조한다(예비대 있음 최대 0.85·0.3 초과 7기 / 없음 0.38·2기).
지운 상태로 돌려 **실제로 실패**하는 것까지 확인했다.

## 13-2. 되짚어야 했던 것 — 게이트를 낮추지 않는다

③④를 고치자 G8-b가 하나 남았다: **정답 95% 아이 패배율 2.86%** (하한 3%). 630판 중 18패,
19패면 통과다. 0.14%p 차이라 임계값을 건드리고 싶은 유혹이 컸지만, 이 게이트는 사용자가
"너무 쉬워서 노잼"이라고 한 바로 그것을 잡으라고 만든 것이다. **게이트가 아니라 게임을 고쳤다.**

원인은 단계 표에 있었다. 단계 간 **배율**이 위로 갈수록 완만해진다:

```
현재:  ×1.70 → 1.53 → 1.38 → 1.33 → 1.29 → 1.26 → 1.23 → 1.21
```

그래서 상위 단계에서 한 칸 올라가도 별로 안 아프고, 적응 루프의 균형점(5.5단계)에서 거의 안 진다.
4단계부터 ×1.35로 고정했다 — **0단계는 손대지 않았다**(G2 안전망의 근거).

결과: 정답 95% 아이 패배율 **2.86% → 5%**, 성 피격 11% → 13%. 전 게이트 통과.

## 13-3. 걸러낸 것

- **codex WARNING "COVER 0.8까지 전원 배출 보장 없음"** — ④를 고치면서 근거가 사라졌다.
  이제 진행도를 넘긴 즉시 나온다.
- **Gemini 카피 대안 "N레벨까지 조각 M개 더!"** — 방향은 채택했지만 문장은 바꿨다.
  `M개 더 모으면 N레벨!`이 목표를 뒤에 두어 저학년에게 더 읽힌다.
- **Claude NIT "shots 상한 41 (off-by-one)"** — 사실이라 고쳤다(`> 40` → `>= 40`).
- **Claude "저사양 프로파일링 권장"** — 코드 리뷰만으로는 근거가 부족하다는 리뷰어 본인의 판단에
  동의한다. 기존 코드도 매 프레임 그래디언트를 만드는 같은 패턴이고, 새 배열은 비활성 시 즉시 반환한다.
  실기기 실측 전까지는 조치하지 않는다.

## 13-4. 이번에도 나온 것 — 주석이 코드보다 많은 것을 주장했다

③이 그렇다. `RESERVE_SHARE` 주석은 "뒤쪽 25%는 진행도로 나온다"고 단언했는데,
가장 흔한 입력 범위(cap 1~3)에서는 **한 기도 안 걸렸다**. 테스트도 통과했다 —
큰 cap 웨이브들이 대신 떠받쳐서 전체 지표가 목표를 만족했기 때문이다.
**집계 지표가 맞으면 구성 요소도 맞다고 믿으면 안 된다.**
"살아남은 것만 재면 전량 삭제를 못 본다"와 같은 함정이다.

# 14. 출전 슬롯 6장 + 답 칸 넘침

실사용자: "출전 캐릭터 수가 더 올릴수는 없나? 이대로도 난이도가 맞음?? 그리고 답을 쓰는 곳이 칸을 넘어감.
오른쪽 숫자 입력 버튼간 패딩을 좌우로 더 줄이면 되지 않아?"

## 14-1. 답 칸이 칸 밖으로 나갔다

`.quiz .q` 는 `white-space: nowrap` 이라 칸보다 길면 **그냥 삐져나간다.** 11절에서 숫자패드를
줄이려고 문제 칸을 좁혔는데 글자 크기는 그대로 뒀다 — 한 칸을 넓히면 다른 칸이 좁아진다는 걸
놓친 것이다. 실측(844×390): `5 × 3` 22px · `12 × 8` 42px · `37 + 25` **61px** 넘침.

→ 칸에 `container-type: inline-size` 를 주고 글자를 `min(4.2vh, 14cqi)` 로 잡았다.
**칸이 자기 폭을 알고 글자를 줄인다** — 앞으로 문항 유형이 늘어도 손댈 필요가 없다.
컨테이너 쿼리 미지원 브라우저는 바로 앞 줄의 `clamp` 로 떨어진다.
겸사겸사 선생님 말씀대로 숫자패드도 더 줄였다(버튼 최소폭 49→44px).

⚠️ **측정을 두 번 틀렸다.**
- `scrollWidth - clientWidth` 로 쟀더니 전부 0이 나왔다 — `overflow: visible` 이면 넘침이
  반영되지 않는다. 넘치는 화면에서도 "여유 있음"으로 읽혔다.
- 요소의 `getBoundingClientRect()` 로 바꿨더니 문항 길이와 무관하게 같은 값이 나왔다 —
  블록 요소의 rect 는 **항상 칸 폭**이라 안의 글자가 얼마나 넘치든 똑같다.
- `Range.selectNodeContents()` 로 **글자가 실제로 차지한 폭**을 재고서야 맞았다.

## 14-2. 출전 슬롯 5 → 6

밸런스부터 재고 답했다. 게이트를 슬롯 5·6·7로 각각 돌린 결과:

| 슬롯 | 정답40% 패배 | 정답60% 최저승률 | 정답85% 패배 | 정답95% 패배 | 게이트 |
|---|---|---|---|---|---|
| 5 | 24% | 86% | 12% | 5% | 통과 |
| **6** | 28% | 86% | 15% | 7% | **통과** |
| 7 | 27% | 86% | 16% | 7% | 통과 |

**슬롯을 늘리면 오히려 조금 어려워진다.** 고를 게 많아지면 비싼 셈지기를 내느라 셈력을 덜
효율적으로 쓰기 때문이다. 안전망(정답 60% 최저 승률 86%)은 셋 다 동일. 7은 6 대비 이득이
없고 화면만 더 먹어서 **6으로 정했다.**

## 14-3. 카드 폭을 고정하면 슬롯이 하나만 늘어도 잘린다

6장으로 늘리자 844×390에서 48px 잘렸다. 카드 폭이 미디어 쿼리마다 `72px / 54px / 48px` 로
**박혀 있었기** 때문이다. → 남은 폭을 장수로 나눠 쓰되 44px(터치 하한) 밑으로는 안 내려간다.

⚠️ CSS 안에서 `100%` 로 계산했더니 트랙이 접혔다 — **순환 참조**다(트랙 폭은 카드 폭에서
나오는데 카드 폭을 다시 트랙 폭에서 구한다). 브라우저가 퍼센트를 0으로 풀어 버린다.
그래서 실제 폭은 `battle.ts` 가 재서 `--deck-space` 로 넣어 준다(ResizeObserver 로 추적 —
창 크기 말고도 회전·주소창 접힘으로 폭이 바뀐다).

## 14-4. `npm run check:layout` — 이 절이 생긴 이유

레이아웃은 **한 칸을 넓히면 다른 칸이 좁아진다.** 이 프로젝트에서 세 번 연속 그랬다:
숫자패드를 줄였더니(11절) → 문제 칸이 넘쳤고(14-1) → 그걸 고쳤더니 덱이 다시 잘렸다(14-3).
전부 코드를 읽어서는 안 보이고 **재야** 보인다.

그래서 실제 브라우저에서 9개 화면을 재는 게이트를 만들었다:
덱 잘림 0 · 가장 긴 문항의 답 칸이 상자 안 · 숫자 버튼 ≥44px · 전장 ≥120px · 가로 스크롤 0.
문제 칸을 일부러 좁혀 **실제로 실패하는 것**까지 확인했다(844×390에서 35px 넘침 검출).

측정 결과(전 화면 통과): 1280×720 · 1194×834 · 1024×600 · 960×462 · 900×420 ·
844×390 · 800×360 · 740×360 · 667×375.
⚠️ 568×320(2016년 SE 가로)은 게이트 대상에서 뺐다 — 6장 × 44px + 패드 + 문제 칸이
물리적으로 안 들어간다. 그 기기에서는 덱이 가로로 스크롤된다.

---

# 15. v3 — 대량 추가 · 광역 · 특별기술 · 재도전 루프 (2026-08-02)

실사용자(교사) 요청 여덟 가지를 한 번에 받았다. 요지는 하나로 모인다 —
**"지금은 처음에 문제만 풀고 한 번에 다 뽑으면 거의 다 깨진다. 긴장감이 없다.
못 깨도 계속 도전하고 업그레이드하면서 못 깨던 판을 깨는 게 게임의 재미다."**

이번 작업의 기준선은 그 문장이다. 그래서 게이트를 하나 새로 만들었다(G9, 아래 15-5).

## 15-1. 무엇을 넣었나

| 요청 | 한 것 |
|---|---|
| 캐릭터 대량 추가 | 셈지기 24 → **38종**(신규 14) |
| 적 대량 추가 | 엉킴괴수 12 → **21종**(신규 9, 수문장 1종 포함) |
| 범위 공격 | `aoe` 를 아군·적 **양쪽**의 정의에 둔다. 겹쳐 선 것은 함께 맞는다 |
| 전설의 특별기술 | `skill: {every, mult, aoe}` — **N번째 타격마다** 터진다(확률 아님) |
| 전설이 자리를 더 차지 | 전장 상한을 마리 수 → **자리 수**로. 노멀·레어 1칸 / 유니크 2 / 에픽 3 / 전설 4 |
| 전설 재생산 느리게 | 전설 쿨다운 26~32초 → **34~44초**, 에픽·유니크도 상향 |
| 먹 대포 하향 + 강화 가능 | 기본 위력 60%(충전도 정답 12개→15개), 상점에서 5단계까지 되사기 |
| 셈력 차오르는 속도 강화 | 상점에 '셈력 샘' 5단계 신설 |
| 모바일 게임창 확대 | 전장이 화면의 49~63% → **58~68%**(15-6) |

## 15-2. 특별기술을 확률로 두지 않은 이유

시뮬은 결정론이어야 프로브·parity 테스트가 성립한다. 그리고 저학년에게는
"몇 대 때리면 나온다"가 운보다 읽기 쉽다 — 리듬이 보이면 기다리는 재미가 생긴다.
때린 횟수는 `swing()` 한 곳에서만 센다. 성을 때릴 때도 같은 카운터를 쓴다 —
안 그러면 전선이 적 성에 닿은 뒤로 전설이 영영 기술을 안 써서
**"샀는데 안 나오는 기술"** 이 된다.

## 15-3. 🔴 지분 방식은 총 체력을 고정하지만 **총 위협은 고정하지 않는다**

`stages.ts` 는 "적 종류를 늘려도 총 난이도가 변하지 않는다"고 적어 두고 있었다.
신규 7종을 넣고 게이트를 돌리자 **G2(저성취 안전망)가 6건 깨졌다** —
정답 60% 아이의 승률이 ST20 95%→24%, ST27 81%→38%.

한 종씩 빼 가며 원인을 귀속했다(`GUGU_SWEEP_SKIP` 노브를 이때 만들었다):

| 뺀 종 | ST20 | ST27 | ST29 |
|---|---|---|---|
| (전부 있음) | 24% | 38% | 43% |
| −하늘연 | 33% | 52% | **67%** |
| −먹구름떼 | 43% | 57% | 62% |
| −무쇠엉킴 | 33% | 52% | 67% |
| **−신규 7종 전부** | **95%** | **81%** | **100%** |

특정 한 종의 문제가 아니었다. 원인은 둘이었다.

**① 사거리·광역은 같은 체력으로 훨씬 세게 싸운다.** 하늘연(사거리 240·공격 62)은
근접 아군이 닿지 못하는 자리에 서서 전선을 계속 갉는다. 이 한 종만으로
정답 40% 아이의 ST18 승률이 33%p 움직였다. → 사거리를 **그 시점 아이가 가진
원거리 셈지기(붕붕이 190)가 닿는 선 아래**로 내렸다.

**② 동시에 나오는 종류 수에 상한이 있다.** 수량은 `max(1, round(...))` 이라
지분이 아무리 작아도 최소 1마리는 나온다. 16종을 동시에 켜면
**"종류당 1마리"만으로 예산이 다 소진된다** — ST27 에서 1마리씩만 세워도
8,580(예산 8,424)이었다. 물량 담당(물음표벌레)이 9마리에서 1마리로 쪼그라들고,
총 위협은 예산 대비 31% 초과에서 53% 초과가 됐다.
→ **옛 괴수는 물러난다**(`WAVES` 에 은퇴 열 추가). 동시 활성 10종 안팎.
부수 효과로 "구역을 넘으니 처음 보는 괴수가 나온다"가 생겼다.

두 가지를 고치자 G2 는 ST15~30 전 구간 86~100% 로 복구됐다.

> **다음에 적을 추가할 사람에게:** 지분만 조정하지 말 것. `GUGU_SWEEP_SKIP` 로
> 원인을 귀속하고, 은퇴표를 함께 손봐야 한다. 지분을 절반으로 줄여도
> `max(1, …)` 하한에 걸려 있으면 **아무 변화도 없다**(실측으로 확인했다).

## 15-4. 난이도 — 사다리를 늘리고 오르는 속도를 올렸다

이전: 정답 95% 아이가 평균 5.1단계에 앉아 30판 중 2판만 졌다(패배율 7%).
천장(8단계)이 낮아 적응 루프가 그 아이를 위협할 자리까지 데려가지 못했다.

- `MAX_TIER` 8 → **12** (공격력·돌파·광역 표 연장)
- **압도 규칙 신설** — 성 무피격 + 정답 90% 이상이면 한 판으로 **두 칸**. 이게 없으면
  잘하는 아이가 자기 단계에 닿기까지 16판이 걸린다(캠페인 30판의 절반).
  어려워하는 아이에게는 발동하지 않는다(정답 90%는 그 아이의 조건이 아니다).
- 무한 구간 배율 1.05 → **1.07**. 이전에는 소환 0 로스터조차 ST31~50 을 전부 100%
  통과했다 — 무한 구간이 벽이 아니라 복도였고, 재도전 루프가 시작될 지점이 없었다.

| 아이 실력 | 머문 단계 | 패배율(전→후) | 성 피격률(전→후) |
|---|---|---|---|
| 정답 60% | 0.1 | 4% → 4% | 6% → 8% |
| 정답 75% | 1.7 | 13% → 15% | 19% → 21% |
| 정답 85% | 3.3 | 15% → 21% | 28% → 33% |
| **정답 95%** | 5.4 | **7% → 23%** | **17% → 32%** |

도달 한계(승률 50% 이상): 캠페인 로스터 ST50→**ST45** · 유니크 ST50 · 전설 ST60.
이제 무한 구간에 **로스터별로 다른 벽**이 있다.

## 15-5. G9 — 재도전 루프 게이트 (신설)

> "못 깨도 계속 도전하고 업그레이드 시키고 하면서 못 깨던 판을 깨는 게 게임의 재미야."

이 문장을 게이트로 옮겼다. G8-c(패배율 상한)는 "너무 어렵진 않은가"만 보고,
G2 는 아예 못 막히는 구간만 본다. **그 사이가 비어 있었다** —
지금 막힌 아이가 전력을 키우면 뚫리는가.

막히는 아이(정답 40%)를 표본으로, 같은 판을 두 로스터로 굴려 비교한다:

| 판 | 소환 0·레벨 1 | 유니크 확보·레벨 5 | 증가폭 |
|---|---|---|---|
| ST18 | 38% | 71% | +33%p |
| ST20 | 14% | 52% | +38%p |
| ST30 | 38% | 95% | +57%p |

판정: 키운 로스터의 승률이 50% 미만이면 "뚫리지 않는 벽", 평균 증가폭이
20%p 미만이면 "성장이 장식". 둘 다 FAIL 이다.

**G8-b 하한도 올렸다.** 예전 하한(패배 3% · 피격 10%)은 통과하면서도
"긴장감이 없다"는 말을 들었다 — 즉 그 선은 재미를 보증하지 못한다.
15% / 10% 로 올리고, 최상위 대역(정답 90%+)에는 별도로 25% / 15% 를 건다.
적응 루프는 균형점을 스스로 찾으므로 중간 대역의 압박은 상수를 아무리 올려도
잘 안 움직인다(실측: 돌파 비율을 30% 올려도 피격률 20→21%). 움직이는 건 천장에 닿는 대역이다.

## 15-6. 모바일 전장 — 원인은 HUD 였다

"모바일에서 실제 게임창 위아래로 더 넓게." 재 보니 원인이 둘이었다.

**① HUD 가 75px 였다.** 안에 있는 '⏸ 잠깐' 버튼이 `.btn.sm`(min-height 60px)을
그대로 물려받고 있었다. 462px 짜리 창에서 상태 표시줄 하나가 세로의 16% 를 먹었다.
HUD 는 메뉴가 아니라 상태 표시줄이므로 자기 크기를 따로 갖게 했다(44px 터치 하한은 유지).

**② 낮은 화면 대응이 430px 부터였다.** 462px 짜리 창에는 **아무 규칙도 안 걸렸다** —
기본값 그대로 3줄 패드에 큰 HUD. 압축 구간을 560px 로 올렸다.

그리고 같은 규칙(패드 6열 배치·버튼 크기·덱 폭)이 세 블록에 흩어져 있어서
한 곳을 고치면 다른 곳이 이겨 아무 변화도 안 나던 것을 한 벌로 합쳤다.

| 화면 | 전장 비중(전→후) |
|---|---|
| 작은 창 960×462 | 51% → **68%** |
| iPhone 14 가로 844×390 | 59% → **66%** |
| 안드로이드 가로 800×360 | 49% → **58%** |
| iPhone SE 가로 667×375 | 50% → **59%** |

`check:layout` 에 **전장 비율 하한(58%)** 을 추가했다. 절대값(120px)만으로는
하단 조작줄이 화면 절반을 먹어도 통과한다 — 실제로 그 상태였다.
실패 시 "HUD ㅁpx + 조작줄 ㅁpx(패드 ㅁ · 덱 ㅁ)" 를 함께 찍는다. 매번 다시 재지 않으려고.

## 15-7. 이번에 헛돌던 것

새 기능마다 회귀 테스트를 쓰고, **기능을 되돌려 빨간불이 뜨는지** 확인했다. 둘이 헛돌았다.

- **방어력**: "센 공격이 약한 공격보다 10배 넘게 들어간다"로 썼는데, 방어력을 통째로
  지워도 통과한다(50 vs 5000 은 그냥 100배다). 공식 그대로 대조하도록 고쳤다.
- **프로브 자리 미러**: emergent 결과(동시 출전 마리 수)만 봐서, 프로브의 소환 판정을
  마리 수로 바꿔 놔도 초록불이었다 — 셈력·쿨다운이 먼저 묶여서 자리 상한에 안 닿았다.
  자리 사용량을 **소환 판정과 따로** 세어 상한 초과를 직접 보게 고쳤다.

그 밖에 테스트가 상수를 베껴 쓰고 있던 것 둘을 상수 참조로 바꿨다
(대포 충전 12회 하드코딩, 덱 길이 5 하드코딩 — 후자는 **실제 버그를 가리고 있었다**:
정규화가 `slice(0, 5)` 라 출전 슬롯을 6장으로 늘린 뒤에도 저장을 다시 읽는 순간
6번째 셈지기가 조용히 사라졌다).

## 15-8. 교차검증에서 잡힌 것 (FULL — codex · Gemini · code-reviewer)

세이브 마이그레이션이 포함돼 중대 변경으로 보고 FULL 로 돌렸다. **세 계열이 독립적으로
같은 결함 두 개를 지목했고, 그 둘은 내가 못 본 것이었다.**

### 채택 (7건)

| # | 지적 | 실측 확인 | 조치 |
|---|---|---|---|
| 1 | 성을 **직접** 때릴 때 특별기술 이벤트가 안 나간다 | `core.ts` 성 분기에 `skill` push 없음 확인 | 이벤트 추가 + **두 경로를 모두 검사하는 테스트**. 되돌려 빨간불 확인 |
| 2 | `tools/` 가 타입 게이트 밖이라 `Battle` 시그니처 변경을 놓쳤다 | `enemy-visibility-probe.ts:24` 가 구 시그니처, `tsconfig.include` 에 `tools` 없음 | 호출부 수정 + `tools` 를 include 에 추가 |
| 3 | **밸런스 게이트가 상점 강화를 전혀 안 본다** | 프로브가 `manaCap(0)`·`regenMult(0)`·`cannonMult(0)` 고정 | `simulate` 에 강화 인자 추가 + **G10 게이트 신설**(아래) |
| 4 | 자리 배지 색 대비 2.29:1 — **전설에서 가장 나쁨** | 금색 #C8A02C on 한지 #faf6ee | 먹빛 칩 위 흰 글자(약 15:1) |
| 5 | 분열 무한증식을 **데이터 하나**가 막고 있다 | 새끼 정의에 `split` 이 없어서 멈출 뿐 | `canSplit=false` 로 코드가 막는다 + `n` 상한 |
| 6 | 프로브가 신규 메카닉을 실제로 도는지 검사할 방법이 없다 | 미러 테스트가 상수·자리·은퇴표만 봄 | 프로브에 **거동 계수기**(armorBlocked·skillHits·splitBorn·aoeHits) + 0이면 실패하는 테스트 |
| 7 | 상점 문구의 `%` 표기 · '벼리기' · '갈래애벌레/갈래벌레' 혼동 · "새끼" | — | 칸(`▮▯`) 표기로 교체, 쉬운 말로, '꼬마갈래'로 개명. 이름 게이트에 반영 |

**7번이 특히 뼈아프다.** 백분율은 초등 6학년 '비와 비율' 내용이다. 이 프로젝트는 이미
같은 이유로 신바람 표시를 "×1.8" 대신 "⚡⚡ 빨라짐"으로 바꾼 전례가 있는데, **새 화면에
같은 실수를 다시 했다.** 전례가 코드 주석에만 있고 검사가 없으면 그 전례는 지켜지지 않는다.

### 오탐으로 거른 것

Gemini 가 프로브 미러에 대해 CRITICAL 1 · WARNING 2 를 냈는데 **셋 다 오탐**이었다
(unified diff 의 `-` 삭제줄을 현재 코드로 읽은 것). 실제 소스를 grep 해 확인 후 기각.
계열별 정밀도 실적상 Gemini 단독 지적은 코드 대조 후에만 채택하는 규칙이 그대로 작동했다.
다만 **같은 Gemini 가 한국어 문구에서는 가장 값진 지적을 냈다** — 계열별로 잘하는 렌즈가 다르다.

### 남긴 것 (알고 두는 격차)

- **틱 시계 불일치**: 코어는 `t += dt` 를 맨 먼저, 프로브는 루프 끝에서 한다. 맞추지 않는다 —
  이 모델은 복제가 아니라 상대 비교 도구이고(타임스텝부터 1/30초 vs 0.1초로 다르다),
  지금 게이트 임계값이 전부 이 시계로 보정돼 있다. 대신 프로브 머리말에 명시했다.
- **숫자 버튼 높이 30~40px**(폭은 44px 유지): 높이에도 44px 를 걸면 낮은 화면에서 전장이
  46% 로 떨어진다(실측 800×360). 사용자가 요구한 건 전장을 넓히는 쪽이라 맞바꿨다.
  대신 `check:layout` 이 버튼 크기를 매번 찍고 하한 30px 를 지킨다 — **실기기 오터치가
  보고되면 되돌릴 지점은 여기다.**

## 15-9. G10 — 먹물 대장간 게이트 (신설)

모든 게이트가 **강화 0단계**로 돈다("가장 불리한 아이가 통과해야 한다"). 옳지만,
그 결과 **끝까지 키운 상태를 아무도 안 봤다.** 최대 강화면 대포 2.17배·샘 1.4배다.

| 판 | 실력 | 0단계 | 최대 강화 |
|---|---|---|---|
| ST20 | 정답 40% | 14% | **33%** |
| ST27 | 정답 40% | 29% | **76%** |

- 최대 강화한 정답 95% 아이: 패배율 23% → **14%**, 성 피격률 32% → **21%** (하한 10%/20% 유지)
- 도달 한계: 캠페인 로스터+최대강화 **ST45** vs 전설 로스터+강화0 **ST55**
  → 대장간만 키워서는 소환을 대체하지 못한다. 두 사용처가 서로를 잡아먹지 않는다.

## 15-10. 그림 — 눈으로 봐야 보이는 것

23장을 뽑아 컷아웃(먹선 flood fill, rembg 아님)까지 마치고 알파 무결성 게이트는 **위반 0**
이었다. 그런데 **한 장으로 붙여서 눈으로 보니 여섯 장이 잘못돼 있었다**:

- `dolhareubang` — **실사 사진**이 나왔다(잔디·하늘까지). 알파 검사는 통과한다.
- `e_splitlet` — 부모(갈래벌레)는 민화 초록인데 새끼는 검정·마젠타. 화풍 이탈.
- `e_armor` — 한 마리가 아니라 세 명이 나왔다.
- `talchum`·`seonnyeo`·`cheollima` — 등급 접미사의 "radiant aura / subtle glow"가
  **마젠타 광채**로 렌더됐다. 기존 전설(구미호·산신령·용왕)에는 없는 색이다.

🔴 **기계 게이트는 "지워졌는가"만 보고 "맞는 그림인가"는 못 본다.** 사진이든 다른 화풍이든
알파는 멀쩡하다. 에셋은 반드시 **한 장으로 붙여 눈으로 확인**해야 한다 —
그리고 그때 비교 대상은 **기존에 이미 배포된 캐릭터들**이다(신규끼리만 보면 다 같이 이상해도 모른다).
재생성 프롬프트에는 부정 지시를 붙였다: 사진 금지 · 마젠타/네온/광배 금지 · 한 명만.

## 15-11. 실브라우저 검수 — 적응 루프가 눈에 보인다

`npm run qa:launch`(정답률 85% · 문항당 3.2초로 8판 연속):

```
1판 win  19문 t=63초  최저성체력 100%   레벨 1.00
2판 win  24문 t=90초  최저성체력 100%   레벨 1.46
3판 win  22문 t=73초  최저성체력 100%   레벨 2.00
4판 win  31문 t=119초 최저성체력 100%   레벨 1.55
5판 lose 17문 t=65초  최저성체력   2%   레벨 2.18   ← 실제로 졌다
6판 win  30문 t=119초 최저성체력 100%   레벨 1.90
7판 win  43문 t=163초 최저성체력 100%   레벨 2.40
8판 win  45문 t=153초 최저성체력 100%   레벨 2.13
콘솔 에러 0 · 실패 0
```

**5판의 패배가 이번 작업의 요약이다.** 1~4판을 성 무피격으로 이기면 '압도' 규칙이
매번 두 칸씩 올려 5판에서 8단계에 닿고, 거기서 진다. 진 다음 판은 7단계로 내려온다.
같은 하네스가 v2 에서는 **8판 전부 무피격 승리**였다 — 이기고 지는 게 아니라 구경이었다.

문항 레벨도 1.00 → 2.40 으로 올라간다(전에는 30판 내내 Lv1 이 589회, Lv3 이상 0회였다).
전투 난이도와 문항 난이도가 같은 단계에 묶여 있으므로, 전투가 조여지면 문제도 같이 올라온다.

**B2 절을 새로 넣었다** — 전설을 쥐어 주고 한 판을 돌려 `fx.skills` 를 읽는다.
시뮬 테스트는 이벤트가 큐에 담기는 것까지만 보는데, 큐에서 렌더러로 넘기는 한 줄이 빠지면
게임 안에서는 아무 일도 안 일어나면서 테스트는 전부 초록이다 — 이번에 성 타격 경로가
정확히 그 상태였다. 실측: 기술 연출 12회 · 원거리 43발 · 최대 사용 자리 10.

---

# 16. 난이도 3차 개편 — "벽이 없었다" (2026-08-07)

사용자 보고: **"아직도 난이도가 너무 쉬워.. 너무 쉬우면 사실 재미가 없다고.. 몇번을 말하니?"**
세 번째 같은 보고다. 앞의 두 번은 손잡이를 조였는데 이번엔 **구조**를 봤다.

## 16-1. 실측이 먼저 — 캠페인에 벽이 하나도 없었다

밸런스 프로브 격자(21시드):

| | 정답 60% | 정답 80% | 정답 95% |
|---|---|---|---|
| ST1~ST30 전 판 | 86~100% | **100%** | **100%** |

정답 80% 이상 아이는 **30판 전부를 한 판도 안 지고** 통과했다. 원인은 두 줄이었다.

## 16-2. 원인 ① — 캠페인의 상대 난이도가 처음부터 끝까지 같다

```ts
export function enemyMult(index: number) { return allyGrowth(index) * … }   // 정확히 상쇄
```

적 배율이 아군 성장과 **정확히 상쇄**되도록 짜여 있었다. 즉 ST1과 ST30의 상대 난이도가
같고, 늘어나는 건 괴수 **종류**뿐이다. 설계 의도는 "난이도는 적응 단계(tier)가 맡는다"였다.

## 16-3. 원인 ② — 적응 단계는 벽을 만드는 장치가 아니라 **지우는** 장치였다

```ts
if (!m.win) return { tier: clampTier(t - 1), streak: 0 };   // 한 판만 져도 즉시 하락
```

벽에 부딪히면 **벽이 스스로 낮아진다.** 사용자가 말한 재미의 정의
("못 깨도 계속 도전하고 업그레이드하면서 못 깨던 판을 깨는 것")를 시스템이 정면으로
막고 있었다. 게다가 승급은 2연승이 필요해(비대칭) 정답 95% 아이가 30판을 다 하고도
**12단계 중 5.4단계**에 앉아 있었다 — 사다리 위쪽 절반을 평생 못 본다.

## 16-4. 시도했다가 되돌린 것 — 캠페인 경사 (기록해 둘 것)

캠페인 안에 판당 ×1.032 경사를 넣어 봤다. 결과:

| | 정답 40% 패배율 | 정답 95% |
|---|---|---|
| 경사 없음 | 38% | 전 판 100% 승 |
| ×1.032 | **74%** | **여전히 전 판 100% 승** |
| ×1.012 | 61% | 여전히 100% 승 |

**전역 배율은 강자에게 안 듣고 약자만 죽인다.** 잘 맞히면 셈력이 많아 유닛을 더 세우고,
적은 성문 앞에서 16대1로 죽을 뿐이다. 이건 `tier.ts` 머리말에 이미 적혀 있던 실측
("적 체력 ×4 → 아무 변화 없음")과 같은 이야기였다 — **읽고도 같은 함정을 다시 팠다.**
난이도를 아이별로 가르는 손잡이는 적응 단계 하나뿐이다. 전량 되돌렸다.

## 16-5. 채택 — 사다리 두 줄만 바꿨다

```ts
export const EASY_STREAK_TO_RAISE = 1;   // 2 → 1  (여유 승 한 판이면 오른다)
export const LOSE_STREAK_TO_DROP  = 2;   // 신설   (한 번 져서는 안 내려간다)
```

연패는 같은 칸에 **음수**로 센다(저장 칸을 늘리지 않으려고).

| 실력 | 머문 단계 (전→후) | 패배율 (전→후) |
|---|---|---|
| 정답 40% | 0.0 → **0.0** | 38% → **38%** |
| 정답 60% | 0.1 → 0.9 | 4% → 26% |
| 정답 75% | 1.7 → 3.0 | 15% → 45% |
| 정답 85% | 3.3 → 4.7 | 21% → 50% |
| 정답 95% | 5.4 → **7.6** | 23% → **40%** |

**어려워하는 아이(40%)는 한 칸도 안 움직였다.** 안전망은 그대로고, 위쪽만 열렸다.

## 16-6. 게이트를 고쳤다 — 이게 이번 작업의 핵심

세 건이 실패했고, 셋 다 "너무 어려워지지 마라"를 지키는 게이트였다.
**사용자가 세 번 반대로 말했으므로 고쳐야 할 것은 밸런스가 아니라 이 임계값이다.**

- **G8-a 안전망**: "정답 60% 이하 전부 0.5단계 이하" → 대역을 갈랐다.
  **40% 이하 = 0.5단계 이하(종전 그대로)**, 60% = 1.2단계 이하.
  60%는 열에 여섯을 맞히는 아이다 — 어려워하는 아이가 아니라 **배우는 중인 아이**인데,
  그 대역까지 0단계에 묶어 두니 패배율 4%(사실상 못 지는 게임)가 됐다.
- **G8-c 패배율 상한**: 45% → 55%. 반은 지는 정도가 이 게임이 노리는 자리다.
  상한을 없애지는 않았다 — 이 게이트가 잡아야 하는 건 "적정 도전"이 아니라
  **되먹임이 폭주해 아무도 못 이기는 상태**다.

지는 게 벌이 아니라 재도전이 되는지는 G9가 따로 잰다(정답 40% 아이 기준
소환·승급으로 ST18 +33%p · ST20 +38%p · ST30 +57%p — 이번 변경 후에도 그대로).

## 16-7. 조용히 개편 전체를 무력화할 뻔한 버그

```ts
streak: Math.floor(num(challenge['streak'], 0, 0, EASY_STREAK_TO_RAISE - 1)),
```

`EASY_STREAK_TO_RAISE` 를 2→1 로 내리는 순간 이 범위가 **[0, 0]** 이 된다. 즉
**연패 카운트가 저장·복구 때마다 0으로 지워져서** "2연패라야 내려간다"가 조용히
"안 내려간다"가 된다. 코드를 읽어서는 안 보이고 게임을 해도 티가 안 난다.
범위를 `[-(LOSE_STREAK_TO_DROP-1), EASY_STREAK_TO_RAISE-1]` 로 고치고,
`tests/save.spec.ts` 에 회귀 3건을 박았다(옛 클램프로 되돌리면 3건 실패 — 변이로 확인).

---

# 17. 휴대폰 가로 자동 전체화면 (2026-08-07)

요청: "모바일에서 어느 브라우저든 실행하면 가로로 하잖아? 그러면 자동으로 풀스크린으로."

## 17-1. "자동"이 문자 그대로는 불가능하다 — 두 가지 제약

**① 전체화면은 사용자 제스처 안에서만 허용된다.** `requestFullscreen()` 은 브라우저의
*user activation* 밖에서 부르면 전부 거부된다 — `orientationchange`·`resize` 핸들러가 바로
그 밖이다. 그래서 **가로로 돌린 뒤 첫 터치**를 기다렸다가 그 순간 요청한다.
어차피 게임을 시작하려면 버튼을 눌러야 하므로 체감상은 자동이다.

**② 아이폰 사파리에는 요소 전체화면 API가 아예 없다.** `requestFullscreen` 도
`webkitRequestFullscreen` 도 iPhone Safari 에는 없다(아이패드는 있다). 스크립트로는
어떤 방법으로도 주소창을 없앨 수 없다 — 그 기기의 유일한 길이 **홈 화면에 추가**라
`apple-mobile-web-app-capable` 등을 index.html 에 넣었다. 기능 감지로 조용히 넘어간다.

## 17-2. 발동 조건 — 셋 다 맞을 때만

| 조건 | 왜 |
|---|---|
| `pointer: coarse` | 마우스 기기에서 페이지가 제멋대로 전체화면이 되면 고장으로 읽힌다 |
| 가로 | 세로엔 "기기를 돌려 주세요" 안내가 떠 있다 — 그걸 꽉 채우면 아무것도 못 한다 |
| 높이 ≤ 700px | 전자칠판·태블릿 거치 화면을 빼앗지 않는다 |

**사용자가 스스로 나간 전체화면은 다시 붙잡지 않는다.** 뒤로가기 제스처로 나간 건 의사
표시다. 다시 무장하는 시점은 *가로로 새로 돌렸을 때* 하나뿐이다.

## 17-3. 게이트가 헛돌던 것을 두 번 잡았다 (`tools/fullscreen-check.mjs`)

호출 여부를 가로채 재는 게이트를 만들고 **조건마다 변이를 넣어** 확인했다.
그 과정에서 **처음 두 케이스가 다른 조건에 먼저 걸려 아무것도 검증하지 못하고 있었다**:

- 포인터 검사를 지웠는데 통과했다 → 데스크톱 케이스(1280×**720**)가 *높이* 조건에
  먼저 걸렸기 때문. **작은 창 데스크톱(1100×600)** 을 추가해서야 포인터 검사가 검증됐다.
- 가로 검사를 지웠는데 통과했다 → 휴대폰 세로(390×**844**)도 *높이* 조건에 먼저 걸렸다.
  **아이폰 SE 세로(375×667)** 는 높이를 통과하므로 가로 검사만이 막는다.

교훈은 [[verify-what-was-deleted-not-what-survived]] 와 같다 — **통과했다는 사실은
그 조건을 검사했다는 뜻이 아니다.** 조건이 여러 개면 케이스마다 *어느 조건이 막는지*를
따져야 하고, 확인 방법은 하나씩 지워 보는 것뿐이다. 지금은 네 조건 전부 변이로 확인된다.

## 17-4. 남은 한계 (정직하게)

- **아이폰 사파리**: 브라우저로 열면 주소창이 남는다. 홈 화면에 추가하면 사라진다.
- **실기기 미검증**: 헤드리스에서는 실제 전환을 신뢰할 수 없어 *호출 조건*만 검증했다.
  실제로 꽉 차는지는 안드로이드 폰·아이패드에서 한 번 눈으로 봐야 한다.

---

# 18. 상용화 수준 업그레이드 (2026-08-12)

요청: "게임성 난이도 중독성 등을 상용화 수준으로 전체 업그레이드."

12개 에이전트로 6개 관점(첫세션·일일복귀·타격감·진행해금·경제가챠·전략깊이)을 병렬 진단하고
각 관점을 반박 검증했다. 36건이 나왔고, 그중 실제로 채택한 것과 **기각한 것**을 함께 남긴다.

## 18-1. 기각한 것 — 검증자가 옳았던 두 건

- **"먹물 총공급이 총수요에 못 미친다"** — 총공급을 캠페인 30판(4,932)으로 잡은 전제가 틀렸다.
  무한 구간에서 새 판을 깰 때마다 같은 공식으로 계속 나온다. 4,932 에 갇혀 있지 않다.
- **"10연 소환이 상시 도달 불가"** — 먹물은 세션마다 리셋되지 않는 **누적 지갑**이다.
  누적으로 계산하면 12판쯤에 이미 1,195 로 1,080 을 넘는다.

두 건 다 산술은 맞았고 **전제가 틀렸다.** 기록해 두는 이유: 다음에 같은 계산을 다시 하게 된다.

## 18-2. 그 반박이 건드리지 못한 것 — 이번 작업의 핵심

먹물이 늘어나는 곳이 코드 전체에서 `src/ui/screens.ts:307` **한 곳**이었고,
지급 조건이 "별이 이전 기록보다 나아진 만큼"이었다. 그래서:

| 상황 | 받는 먹물 |
|---|---|
| 졌다 | **0** (관문에 못 간다 — `main.ts` 의 win 분기) |
| 이미 3별인 판을 다시 이겼다 | **0** (`max(0, 60-60)`) |

즉 **막힌 아이에게는 수입 경로가 존재하지 않았다.** 전력을 올리려면 먹물이 필요한데
먹물을 벌려면 이겨야 하고, 이기려면 전력이 필요하다 — 순환이다.
"무한 구간에서 벌면 된다"는 반박은 **새 판을 이겨야** 성립하는데, 막힌 아이는 그걸 못 한다.

그리고 §16 에서 패배율을 40~50% 로 올려 놓은 뒤라 더 아팠다.
`stages.ts` 머리말은 "먹물은 정답에서만 나온다"고 선언하는데 실제로는 별에서 나왔다 —
**선언과 구현이 어긋나 있었다.**

→ `matchInk(correct, stage)` 신설. 맞힌 문제 수만큼, **승패 무관** 지급.
   파밍 걱정은 여기서 뒤집힌다 — 이 게임에서 반복 플레이는 **연산 문제를 더 푸는 것**이다.

## 18-3. G11 — 자력 탈출 게이트 (신설)

G9 는 "유니크를 레벨 5로 들고 있으면 뚫린다"를 잰다. **그걸 어떻게 갖는지는 묻지 않는다.**
그래서 위 상태가 G9 초록불로 통과하고 있었다. G11 은 그 사이를 잰다 —
막힌 아이가 *자기가 번 먹물만으로* 벽을 넘는가.

게이트를 만들며 **세 번 틀렸고 세 번 다 게이트가 알려 줬다**:

1. **잘못된 대상을 쟀다.** 정답 40% 아이의 ST18·20 을 벽으로 잡았는데, G2 는 60% 를
   안전선으로 두고 G10 은 이미 "ST20·정답40% 는 최대강화로도 33%"라고 보고하고 있었다 —
   그 조건은 강화로 뚫리지 않도록 **설계된** 지점이다. 진짜 벽은 무한 구간에 있다(ST40).
2. **소비 정책이 틀렸다(1)** — 소환 120 이 최저가 강화 180 보다 싸서 지갑이 늘 소환으로
   먼저 새어 나가 강화를 영영 못 샀다(25판에 강화 1칸). 아이는 저축을 한다.
3. **소비 정책이 틀렸다(2)** — 반대로 "강화 먼저 다 사고 소환"으로 두니, 먹물을 **3배로
   줘도 승률이 24%→33% 로 똑같았다**(강화 12칸 · 셈지기 +0종). 강화는 6·5·5 단계 천장이
   있어 효과가 유계고, 무한 구간의 벽은 `stages.ts` 말대로 **소환·승급**으로 넘는 곳이다.

최종: 강화와 소환을 번갈아 산다. 실측 **ST40 · 시작 승률 24% → 20판 도전 후 48%**.

게이트 기준도 이 과정에서 내렸다. 처음엔 "20판 안에 50% 돌파"였는데, 위 3번이 보여 주듯
그건 이 게임의 설계가 아니고 강제하면 경제를 부풀려 캠페인을 망가뜨린다.
**이 게이트가 지켜야 하는 건 막히지 않는 것이지 빨리 뚫리는 것이 아니다** —
① 수입 > 0 ② 20판에 승률 +8%p 이상.

## 18-4. 타격감 (진단 최고점 묶음)

- **정답 순간 캔버스 피드백이 0 이었다**(영향 7/10). 맞히면 문제 상자에 작은 글씨가 뜰 뿐,
  전장은 무반응. 반대로 **틀리면 상자가 흔들린다** — 잘한 것보다 못한 것에 화면이 더 크게
  반응하는 역설이었다. 게임의 한 줄 컨셉이 "계산이 빨라질수록 내 군대가 강해진다"인데
  그 인과가 화면에 없었다. → `renderer.cheer()` — 성 위로 솟는 먹물, 콤보에 따라 크기·색 상승.
- **콤보 시청각 상승감 없음**(영향 6) → `playCorrect(combo)` 가 반음씩 올린다(한 옥타브에서 멈춤).
  **새 오디오 파일 없이** 소리 6종을 사실상 수십 종으로 늘렸다.
- **히트스톱·넉백 전무**(영향 6) → `hitstop()`. 🔴 시뮬 시계는 안 건드린다 —
  이 프로젝트는 '화면 배속'으로 정확히 그 함정을 밟아 같은 판의 문항 수가 줄었다(§학습량 게이트).
  **그리기 변환만** 한 프레임 확대한다.
- **처치 이펙트 등급 무차별**(영향 4) → `puff(x, side, big)`. 아군은 자리 수, 적은 고정형/맷집 기준.
- **보스 등장 연출 없음**(영향 3) → 이 게임에서 **고정형(spd 0)은 수문장뿐**이라 그 조건으로
  정확히 잡힌다. 이름을 크게 띄우고 낮은 소리 + 화면 흔들림.
- **reduceMotion 이 정보까지 삭제**(영향 4) → 동작을 줄이라는 설정이지 정보를 지우라는
  설정이 아니다. 환호는 **떠오르는 움직임만** 죽이고 글자는 남긴다.

## 18-5. 그 밖에 고친 것

- **패배 원인 진단**(영향 6) — 결과 화면이 왜 졌는지 한 가지만 짚고 할 일 하나를 준다.
  전투가 신호를 남기게 `drySec`(셈력이 말라 있던 시간)·`leaked`(성까지 새어 온 적)를 계측한다.
  🔴 `cheapestCost` 기본값을 **0** 으로 둔다(Infinity 아님) — Infinity 면 덱을 안 넘긴 호출에서
  판 전체가 마른 것으로 집계된다. 모르면 보고하지 않는 쪽으로 실패한다.
- **결과 화면의 `%` 2곳 제거** — 백분율은 6학년 '비와 비율'이다. 대장간에서 이미 같은 이유로
  게이지로 바꿔 놓고 여기만 남아 있었다. `맞힌 문제 23개 / 27개` 로.
- **캠페인 클라이맥스(27~30)에 해금이 하나도 없었다**(영향 5) — 마지막이 26판이었다.
  28판 백호(유니크), 30판 주작(에픽) 추가. 최종 수문장을 깨면 보상이 있다.
- **별 3개 기준이 화면 어디에도 없었다**(영향 4) — "맞힐수록 별을 더 받아요"는 목표가 아니다.
  관문에 `5개 다 맞히면 ⭐⭐⭐ / 3개 이상 ⭐⭐` 를 숫자로 적었다.
- **메뉴에 '오늘 할 것'이 없고 SRS 예정일이 계산만 되고 안 보였다**(각 영향 4) —
  이미 있는 최고의 복귀 신호를 버리고 있었다. 복습 → 새 판 → 기록 갱신 순으로 **하나만** 제시한다.
  🔴 접속 연속일(streak) 류는 넣지 않았다 — 아동 대상에 손실 회피를 동력으로 쓰지 않는다
  (교차검증에서도 부적절 판정).
- **무한 구간에 목표 부재**(영향 4) — 자기 최고 기록을 목표로 준다. 남이 아니라 어제의 자신과.
- **ISO 날짜 노출**(영향 2) — `2026-08-19` → `오늘/내일/모레/N일 뒤`.
